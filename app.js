const companies = [
  {name:'Mekong AgriTech',industry:'Agriculture',type:'Startup',membership:'Premium',location:'Phnom Penh',desc:'Smart irrigation and farm analytics helping Cambodian growers improve yield and reduce water use.',tags:['AgriTech','IoT','Growth'],color:'#3ca37b',score:92,approved:true},
  {name:'Koompi Digital',industry:'Technology',type:'SME',membership:'Premium',location:'Phnom Penh',desc:'Local technology company building accessible devices, software, and digital education tools.',tags:['Software','Education','Verified'],color:'#4267b5',score:89,approved:true},
  {name:'Sabay Logistics',industry:'Logistics',type:'SME',membership:'Standard',location:'Kandal',desc:'Technology-enabled last-mile delivery and fulfillment services for Cambodian merchants.',tags:['Logistics','B2B','Scaling'],color:'#e28a45',score:86,approved:true},
  {name:'Tonle Finance',industry:'Fintech',type:'Startup',membership:'Standard',location:'Phnom Penh',desc:'Inclusive financial tools and cash-flow services designed for small and growing businesses.',tags:['Fintech','SME','Seed'],color:'#6758d8',score:84,approved:true},
  {name:'Impact Asia Capital',industry:'Investment',type:'Venture Capital',membership:'VIP',location:'Singapore / Cambodia',desc:'Impact investment firm backing sustainable, scalable ventures across Southeast Asia.',tags:['VC','Impact','Series A'],color:'#253652',score:81,approved:true},
  {name:'BrightPath Academy',industry:'Education',type:'Service Provider',membership:'Standard',location:'Siem Reap',desc:'Business and investment-readiness training for founders, SMEs, and emerging leaders.',tags:['Training','Mentorship','Partner'],color:'#d85f7b',score:76,approved:true},
  {name:'UNI Holding',industry:'Business Services',type:'Strategic Partner',membership:'VIP',location:'Phnom Penh',desc:'Business building, investment facilitation, ecosystem development, and joint ventures.',tags:['Investment','Advisory','Joint Venture'],color:'#5b4bdb',score:91,approved:true},
  {name:'Ballangk Mall',industry:'Commerce',type:'Strategic Partner',membership:'VIP',location:'Phnom Penh',desc:'Market access, seller onboarding, commerce distribution, and SME growth support.',tags:['Commerce','Distribution','SME'],color:'#d77a3d',score:87,approved:true},
  {name:'PiNEX Pro',industry:'Business Services',type:'Service Provider',membership:'Premium',location:'Phnom Penh',desc:'Media, events, communications, and investor-facing business content.',tags:['Media','Events','Marketing'],color:'#bd5575',score:82,approved:true}
];

const directorySeed={
  roots:['Angkor','Mekong','Tonle','Khmer','Bayon','Lotus','Borei','Sovann','Bamboo','Cardamom','Kirirom','Bassac','Apsara','Pailin','Kampuchea','Emerald','Golden Rice','Palm Tree'],
  descriptors:['Digital','Agro','Commerce','Logistics','Finance','Learning','Solutions','Industries'],
  industries:['Technology','Agriculture','Commerce','Logistics','Fintech','Education','Business Services','Investment'],
  locations:['Phnom Penh','Siem Reap','Battambang','Kandal','Kampong Cham','Sihanoukville','Kampot','Takeo','Banteay Meanchey','Kampong Speu','Pursat','Svay Rieng'],
  types:['SME','Startup','Member Company','Service Provider','Manufacturer','Distributor'],
  memberships:['Standard','Premium','Standard','VIP','Standard','Premium','Guest'],
  colors:['#173f96','#2f7f70','#d08635','#6758d8','#bf5575','#3975a8','#6b7b3f','#945f38']
};
const industryTags={Technology:['Software','Digital','Innovation'],Agriculture:['Agriculture','Processing','Sustainable'],Commerce:['Retail','Distribution','Market Access'],Logistics:['Transport','Warehousing','Supply Chain'],Fintech:['Finance','Payments','SME'],Education:['Training','Skills','Learning'],['Business Services']:['Consulting','Professional','B2B'],Investment:['Capital','Advisory','Growth']};
companies.push(...Array.from({length:144},(_,index)=>{const industry=directorySeed.industries[index%directorySeed.industries.length],root=directorySeed.roots[index%directorySeed.roots.length],descriptor=directorySeed.descriptors[Math.floor(index/directorySeed.roots.length)%directorySeed.descriptors.length];return {name:`${root} ${descriptor} ${String(index+1).padStart(3,'0')}`,industry,type:directorySeed.types[index%directorySeed.types.length],membership:directorySeed.memberships[index%directorySeed.memberships.length],location:directorySeed.locations[index%directorySeed.locations.length],desc:`Cambodian ${industry.toLowerCase()} business providing reliable products, professional services, and growth opportunities for local and regional customers.`,tags:industryTags[industry],color:directorySeed.colors[index%directorySeed.colors.length],score:65+(index%31),approved:true}}));

let pitches = [
  {name:'Mekong AgriTech',industry:'Agriculture',stage:'Growth stage',complete:100,score:92,status:'Investor Ready',date:'27 Jun 2026',color:'#3ca37b'},
  {name:'Koompi Digital',industry:'Technology',stage:'Early revenue',complete:95,score:89,status:'Investor Ready',date:'26 Jun 2026',color:'#4267b5'},
  {name:'Sabay Logistics',industry:'Logistics',stage:'Growth stage',complete:90,score:82,status:'Strong Potential',date:'24 Jun 2026',color:'#e28a45'},
  {name:'Tonle Finance',industry:'Fintech',stage:'Early revenue',complete:86,score:78,status:'Strong Potential',date:'23 Jun 2026',color:'#6758d8'},
  {name:'GreenBuild Cambodia',industry:'Construction',stage:'Pre-revenue',complete:72,score:64,status:'Need Improvement',date:'22 Jun 2026',color:'#5a9c8b'},
  {name:'KhmerCraft Market',industry:'Commerce',stage:'Established SME',complete:68,score:59,status:'Need Improvement',date:'20 Jun 2026',color:'#cc735b'}
];

const events = [
  {day:'12',month:'JUL',type:'Flagship event',title:'Cambodia Investment Forum 2026',place:'Sofitel Phnom Penh • 8:30 AM',description:'Meet investors, business leaders, public institutions, and founders shaping Cambodia’s next growth chapter.',accepted:284,maybe:46,pending:31,theme:'',public:true,capacity:500},
  {day:'25',month:'JUL',type:'Demo day',title:'SME Growth & Investment Pitch',place:'Factory Phnom Penh • 1:00 PM',description:'Watch investment-ready SMEs present their growth plans and connect with capital and strategic partners.',accepted:126,maybe:18,pending:42,theme:'green-cover',public:true,capacity:250},
  {day:'08',month:'AUG',type:'Workshop',title:'Investment Readiness Bootcamp',place:'Online + IDI Center • 9:00 AM',description:'A practical workshop on pitch decks, financial preparation, valuation, and investor conversations.',accepted:82,maybe:12,pending:25,theme:'orange-cover',public:true,capacity:150}
];

let newsItems = [
  {headline:'New investment incentives for priority sectors announced',tagline:'The new framework offers qualified projects tax incentives and streamlined investment approvals.',details:'PHNOM PENH — A new package of investment incentives has been announced for priority sectors, with a focus on technology, agriculture, logistics and the green economy.\n\nQualified projects may benefit from faster review, tax support and coordinated assistance from relevant public institutions. Businesses should review the eligibility requirements before preparing an application.\n\nIDI APP members will receive additional guidance as implementation details become available.',category:'Government & Policy',hashtags:['#Cambodia','#Investment','#Policy'],author:'IDI Newsroom',dateline:'Phnom Penh, Cambodia',publishAt:'2026-06-30T08:30',imageCaption:'New policy measures aim to support priority investment sectors.',sourceType:'Government',source:'Council for the Development of Cambodia',level:3,audiences:['VIP'],channels:['app','email','sms'],release:'Published now'},
  {headline:'Impact Asia Capital opens $25M Cambodia growth fund',tagline:'The regional fund is seeking scalable Cambodian businesses with clear expansion potential.',details:'Impact Asia Capital has opened a new $25 million growth fund focused on high-potential companies in Cambodia and neighboring markets.\n\nThe fund will consider businesses in climate, financial technology, logistics and digital services. Initial investment tickets are expected to range from $250,000 to $2 million.\n\nFounders with proven revenue, strong governance and a regional growth plan can submit their profile through IDI APP.',category:'Capital Markets',hashtags:['#VentureCapital','#GrowthFund','#ASEAN'],author:'Investment Desk',dateline:'Phnom Penh, Cambodia',publishAt:'2026-06-30T07:52',imageCaption:'Impact Asia Capital launches a new regional growth mandate.',sourceType:'Investor',source:'Impact Asia Capital',level:2,audiences:['VIP','Premium'],channels:['app','email'],release:'Published 38m ago'},
  {headline:'SME co-investment facility accepting applications',tagline:'Eligible SMEs can now apply for matched capital and investment-readiness support.',details:'Khmer Enterprise has opened applications for an SME co-investment facility designed to help qualified businesses access growth capital.\n\nThe program combines commercial investment with technical support, including financial preparation, governance and market expansion.\n\nApplications will be reviewed against business traction, team capability and the potential to create sustainable economic impact.',category:'Investment',hashtags:['#SME','#CoInvestment','#Cambodia'],author:'Business News Team',dateline:'Phnom Penh, Cambodia',publishAt:'2026-06-30T04:30',sourceType:'Government',source:'Khmer Enterprise',level:2,audiences:['VIP','Premium','Standard'],channels:['app','email'],release:'Published 4h ago'},
  {headline:'Regional logistics expansion creates partnership opportunities',tagline:'A new cross-border network is seeking Cambodian operators, suppliers and technology partners.',details:'Mekong Growth Partners is supporting a regional logistics expansion that will connect operators across Cambodia, Vietnam and Thailand.\n\nThe initiative is expected to create partnership opportunities in warehousing, last-mile delivery, fleet technology and trade services.\n\nInterested members can review the opportunity and request an introduction through the partner directory.',category:'Business',hashtags:['#Logistics','#Partnership','#Mekong'],author:'Regional Markets Desk',dateline:'Phnom Penh, Cambodia',publishAt:'2026-06-29T16:00',sourceType:'Investor',source:'Mekong Growth Partners',level:1,audiences:['VIP','Premium','Standard','Guest'],channels:['app','email'],release:'Published yesterday'},
  {headline:'Cambodia Investment Forum opens public registration',tagline:'Business leaders, investors, founders and professionals can now reserve their place for the July forum.',details:'Public registration is now open for the Cambodia Investment Forum 2026, bringing together investors, entrepreneurs, government representatives and regional business leaders.\n\nThe program will feature market outlook sessions, investor panels, business matching and practical discussions on accessing capital.\n\nVisitors can register directly through the public events section of IDI APP. Membership is not required.',category:'Events',hashtags:['#InvestmentForum','#Cambodia','#Networking'],author:'IDI Events Desk',dateline:'Phnom Penh, Cambodia',publishAt:'2026-06-28T10:00',sourceType:'IDI Association',source:'IDI APP',level:1,audiences:['VIP','Premium','Standard','Guest'],channels:['app','email'],release:'Published 2 days ago'},
  {headline:'Cambodian SMEs gain new regional market access program',tagline:'The public initiative will connect qualified local suppliers with commerce and distribution partners across ASEAN.',details:'A new regional market access program has opened for Cambodian small and medium-sized businesses seeking distribution and commercial partnerships.\n\nSelected companies will receive export preparation support, buyer introductions and opportunities to present products to regional partners.\n\nBusinesses can review eligibility information and prepare a complete company profile in the IDI APP directory.',category:'Business',hashtags:['#SME','#MarketAccess','#ASEAN'],author:'Business News Team',dateline:'Phnom Penh, Cambodia',publishAt:'2026-06-27T14:30',sourceType:'Partner',source:'IDI APP',level:1,audiences:['VIP','Premium','Standard','Guest'],channels:['app','email'],release:'Published 3 days ago'}
];

const members=[
  {name:'Sokha Kim',email:'sokha@uniholding.com',org:'UNI Holding',tier:'VIP',status:'Active',renewal:'28 Jun 2027',channels:'App, Email, SMS',verified:true},
  {name:'Maly Sok',email:'maly@mekongagri.com',org:'Mekong AgriTech',tier:'Premium',status:'Active',renewal:'14 Mar 2027',channels:'App, Email',verified:true},
  {name:'Vattanac R.',email:'vattanac@koompi.com',org:'Koompi Digital',tier:'Premium',status:'Active',renewal:'02 Feb 2027',channels:'App, Email',verified:true},
  {name:'Dara Lim',email:'dara@sabaylogistics.com',org:'Sabay Logistics',tier:'Standard',status:'Active',renewal:'18 Dec 2026',channels:'App, Email',verified:true},
  {name:'Srey Mom',email:'srey@brightpath.edu',org:'BrightPath Academy',tier:'Standard',status:'Expiring',renewal:'12 Jul 2026',channels:'Email',verified:true},
  {name:'Chan Vuthy',email:'vuthy@khmertrade.com',org:'Khmer Trade Hub',tier:'Premium',status:'Expired',renewal:'12 Jun 2026',channels:'App, Email'},
  {name:'Linda Chea',email:'linda@greencambodia.com',org:'Green Cambodia',tier:'Standard',status:'Overdue',renewal:'20 May 2026',channels:'Email'},
  {name:'Chenda Pov',email:'chenda@gmail.com',org:'Independent',tier:'Guest',status:'Active',renewal:'—',channels:'App'}
];

const memberFirstNames=['Sokha','Dara','Sophea','Vireak','Maly','Ratha','Sreypov','Vattanac','Chenda','Bopha','Piseth','Sothea','Sokun','Nary','Ravy','Thida'];
const memberLastNames=['Chan','Lim','Chea','Keo','Touch','Vong','Kim','Pov','Heng','San'];
members.push(...Array.from({length:160},(_,index)=>{const name=`${memberFirstNames[index%memberFirstNames.length]} ${memberLastNames[Math.floor(index/memberFirstNames.length)%memberLastNames.length]}`,tier=index%17===0?'VIP':index%5===0?'Premium':index%7===0?'Guest':'Standard',status=index%29===0?'Overdue':index%23===0?'Expired':index%13===0?'Expiring':'Active',company=companies[9+(index%144)];return {name,email:`member${String(index+1).padStart(3,'0')}@idi-demo.org`,org:tier==='Guest'?'Independent':company.name,tier,status,renewal:tier==='Guest'?'—':status==='Expired'?'15 Jun 2026':status==='Overdue'?'20 May 2026':status==='Expiring'?'12 Jul 2026':`${String((index%27)+1).padStart(2,'0')} Dec 2026`,channels:tier==='VIP'?'App, Email, SMS':'App, Email',verified:tier!=='Guest'&&['Active','Expiring'].includes(status)&&index%4!==0}}));

const loginProfiles={
  admin:{name:'Website Administrator',email:'admin@idiapp.org',password:'Admin@2026',tier:'Admin',role:'Super Admin',organization:'IDI APP',isAdmin:true,canPublish:true,canCampaign:true,allowed:['overview','directory','news','partners','investment-flow','investment-operations','funding-request','memberships','pitching','events','pipeline','messages','reports','staff']},
  vip:{name:'Sokha Kim',email:'sokha@uniholding.com',tier:'VIP',role:'Executive Member',organization:'UNI Holding',membershipExpires:'2026-07-28',paymentStatus:'Paid',verified:true,canPublish:true,allowed:['overview','directory','news','partners','investment-flow','investment-operations','funding-request','memberships','billing','pitching','events','pipeline','messages','reports']},
  premium:{name:'Maly Sok',email:'maly@mekongagri.com',tier:'Premium',role:'Company Founder',organization:'Mekong AgriTech',membershipExpires:'2026-07-07',paymentStatus:'Paid',verified:true,allowed:['overview','directory','news','partners','investment-flow','investment-operations','funding-request','billing','pitching','events','messages']},
  standard:{name:'Dara Lim',email:'dara@sabaylogistics.com',tier:'Standard',role:'Company Member',organization:'Sabay Logistics',membershipExpires:'2026-06-30',paymentStatus:'Paid',verified:true,allowed:['overview','directory','news','investment-flow','investment-operations','billing','events','messages']},
  guest:{name:'Chenda Pov',email:'chenda@gmail.com',tier:'Guest',role:'Guest Member',organization:'Independent',allowed:['overview','directory','news','billing','events']},
  vc:{name:'Rina Chen',email:'rina@impactasia.vc',tier:'VIP',role:'Venture Capital Partner',organization:'Impact Asia Capital',membershipExpires:'2026-06-23',canCampaign:true,allowed:['overview','directory','news','partners','investment-flow','investment-operations','billing','pitching','events','pipeline','messages','reports']},
  strategic:{name:'Vireak S.',email:'vireak@ballangk.com',tier:'Premium',role:'Strategic Partner',organization:'Ballangk Mall',membershipExpires:'2026-05-20',canCampaign:true,allowed:['overview','directory','news','partners','investment-flow','investment-operations','billing','pitching','events','messages','reports']}
};
let currentMember=null;

function isCompanyVerified(company){
  return company?.verified===true||(company?.approved===true&&company?.profileComplete!==false&&company?.documentsVerified!==false&&company?.paymentVerified!==false&&company?.adminVerified!==false);
}
function isMemberVerified(member){return member?.verified===true}
function verifiedBadge(kind='company'){
  return `<span class="verified-badge ${kind==='member'?'member-verified':''}" title="IDI verified: requirements complete and approved by an administrator" aria-label="IDI verified">✓</span>`;
}

const partnerOrganizations = [
  {name:'Impact Asia Capital',type:'Venture Capital',location:'Singapore / Cambodia',sectors:['Green Economy','Fintech','Technology'],focus:'Scalable ventures creating measurable economic and environmental impact.',ticket:'$250K–$2M',stage:'Seed to Series A',capability:'Growth capital',color:'#253652'},
  {name:'Mekong Growth Partners',type:'Venture Capital',location:'Cambodia / Vietnam',sectors:['Logistics','Agriculture','Technology'],focus:'Regional businesses with proven revenue and cross-border expansion potential.',ticket:'$500K–$5M',stage:'Growth stage',capability:'Regional expansion',color:'#16846c'},
  {name:'UNI Holding',type:'Strategic Partner',location:'Phnom Penh',sectors:['Technology','Fintech','Logistics'],focus:'Business building, investment facilitation, ecosystem development, and joint ventures.',ticket:'Strategic',stage:'All stages',capability:'Joint ventures',color:'#5b4bdb'},
  {name:'Ballangk Mall',type:'Strategic Partner',location:'Phnom Penh',sectors:['Agriculture','Logistics'],focus:'Market access, seller onboarding, commerce distribution, and SME growth.',ticket:'Commercial',stage:'Revenue stage',capability:'Distribution',color:'#d77a3d'},
  {name:'PiNEX Pro',type:'Strategic Partner',location:'Cambodia',sectors:['Technology'],focus:'Media, event production, communications, and investor-facing content.',ticket:'Service',stage:'All stages',capability:'Media & events',color:'#bd5575'},
  {name:'Cambodia Innovation Fund',type:'Venture Capital',location:'Phnom Penh',sectors:['Technology','Agriculture','Fintech'],focus:'Cambodian founders building technology-enabled solutions for local and regional markets.',ticket:'$100K–$1M',stage:'Pre-seed to Seed',capability:'Early-stage capital',color:'#4267b5'}
];

let activePublicPartnerFilter='all';
function renderPublicPartners(){const rows=partnerOrganizations.filter(partner=>partner.verified!==false&&(activePublicPartnerFilter==='all'||partner.type===activePublicPartnerFilter));document.getElementById('publicPartnerGrid').innerHTML=rows.map(partner=>{const index=partnerOrganizations.indexOf(partner);return `<article class="public-partner-card"><div class="public-partner-head"><span class="public-partner-logo" style="background:${partner.color}">${initials(partner.name)}</span><div><h3>${partner.name}</h3><p>${partner.type} • ${partner.location}</p></div><span class="public-partner-verified">●</span></div><p class="public-partner-focus">${partner.focus}</p><div class="tags">${partner.sectors.map(sector=>`<span class="tag">${sector}</span>`).join('')}</div><div class="public-partner-facts"><div><span>Investment / value</span><strong>${partner.ticket}</strong></div><div><span>Preferred stage</span><strong>${partner.stage}</strong></div><div><span>Core capability</span><strong>${partner.capability}</strong></div></div><button class="secondary public-partner-button" data-public-partner="${index}">View organization →</button></article>`}).join('');document.getElementById('publicPartnerCount').textContent=`Showing ${rows.length} verified ${activePublicPartnerFilter==='all'?'capital and strategic partners':activePublicPartnerFilter.toLowerCase()+' organizations'}`;document.querySelectorAll('[data-public-partner]').forEach(button=>button.onclick=()=>openPublicPartner(Number(button.dataset.publicPartner)))}
document.getElementById('publicPartnerFilters').onclick=e=>{if(!e.target.dataset.publicPartnerFilter)return;activePublicPartnerFilter=e.target.dataset.publicPartnerFilter;document.querySelectorAll('[data-public-partner-filter]').forEach(button=>button.classList.toggle('active',button===e.target));renderPublicPartners()};
const publicPartnerModal=document.getElementById('publicPartnerModal');
function openPublicPartner(index){const partner=partnerOrganizations[index];if(!partner)return;if(typeof ensurePartnerLogos==='function')ensurePartnerLogos();const services=[partner.capability,'Business introductions','Campaign collaboration','Ecosystem access'],logoMarkup=typeof partnerLogoMarkup==='function'?partnerLogoMarkup(partner,'public-partner-profile-logo'):`<span class="public-partner-profile-logo" style="background:${partner.color};background-image:url('${partner.logo||''}')">${partner.logo?'':initials(partner.name)}</span>`;document.getElementById('publicPartnerProfile').innerHTML=`<header class="public-partner-profile-head">${logoMarkup}<div><h2>${partner.name}</h2><p>✓ Verified ${partner.type} • ${partner.location}</p></div></header><div class="public-partner-profile-body"><div><h3>Organization overview</h3><p>${partner.focus}</p><div class="partner-profile-sectors">${partner.sectors.map(sector=>`<span>${sector}</span>`).join('')}</div><h3>How this partner supports businesses</h3><div class="partner-profile-services">${services.map(service=>`<div><strong>${service}</strong><span>Available through the IDI APP ecosystem</span></div>`).join('')}</div></div><aside class="partner-profile-side"><h3>Investment profile</h3><dl><dt>Organization type</dt><dd>${partner.type}</dd><dt>Investment / value</dt><dd>${partner.ticket}</dd><dt>Preferred stage</dt><dd>${partner.stage}</dd><dt>Primary markets</dt><dd>${partner.location}</dd><dt>Focus sectors</dt><dd>${partner.sectors.join(', ')}</dd></dl><button class="primary partner-public-login">Member login to connect</button></aside></div>`;publicPartnerModal.classList.add('open');publicPartnerModal.setAttribute('aria-hidden','false');publicPartnerModal.querySelector('.partner-public-login').onclick=()=>{closePublicPartner();openLogin()}}
function closePublicPartner(){publicPartnerModal.classList.remove('open');publicPartnerModal.setAttribute('aria-hidden','true')}
document.querySelectorAll('[data-public-partner-close]').forEach(button=>button.onclick=closePublicPartner);publicPartnerModal.onclick=e=>{if(e.target===publicPartnerModal)closePublicPartner()};

const companyExtras={
  'Mekong AgriTech':{founded:'2021',employees:'28',registration:'KH-2021-08421',website:'mekongagritech.com',email:'hello@mekongagritech.com',phone:'+855 23 880 421',products:['Smart Irrigation Controller','Farm Analytics Dashboard','Soil Sensor Kit','Crop Advisory App'],services:['Farm digitization','Installation & training','Data analytics','Technical support'],markets:'Cambodia, Vietnam, Laos',stage:'Growth / Series A',revenue:'$500K–$1M',funding:'Seeking $1.2M'},
  'Koompi Digital':{founded:'2018',employees:'54',registration:'KH-2018-03419',website:'koompi.com',email:'business@koompi.com',phone:'+855 23 991 110',products:['KOOMPI laptops','KOS operating system','Digital classroom kits'],services:['Software development','Education technology','Device deployment','IT training'],markets:'Cambodia and Southeast Asia',stage:'Growth',revenue:'$1M–$5M',funding:'Strategic partnerships'},
  'Sabay Logistics':{founded:'2019',employees:'92',registration:'KH-2019-11873',website:'sabaylogistics.com',email:'sales@sabaylogistics.com',phone:'+855 23 700 800',products:['Merchant fulfillment portal','Delivery tracking API'],services:['Last-mile delivery','Warehousing','E-commerce fulfillment','Cross-border logistics'],markets:'Cambodia nationwide',stage:'Scaling',revenue:'$1M–$5M',funding:'Seeking $2M'},
  'Impact Asia Capital':{founded:'2017',employees:'18',registration:'SG-2017-VC228',website:'impactasiacapital.com',email:'invest@impactasiacapital.com',phone:'+65 6800 2210',products:['Growth Fund I','Climate Impact Fund'],services:['Growth capital','Board advisory','Regional expansion','Follow-on fundraising'],markets:'Southeast Asia',stage:'Seed to Series A',revenue:'$25M AUM',funding:'Deploying $250K–$2M'},
  'UNI Holding':{founded:'2020',employees:'35',registration:'KH-2020-06782',website:'uniholding.com',email:'connect@uniholding.com',phone:'+855 23 888 168',products:['VentureConnect','Business ecosystem programs'],services:['Investment facilitation','Joint ventures','Business advisory','Market access'],markets:'Cambodia and ASEAN',stage:'Strategic group',revenue:'Private',funding:'Co-investment opportunities'}
};
const defaultExtra={founded:'2020',employees:'10–50',registration:'Verified association member',website:'Available on request',email:'Contact through platform',phone:'Available to members',products:['Primary business products','Digital solutions'],services:['Business services','Consulting and support'],markets:'Cambodia',stage:'Operating company',revenue:'Private',funding:'Open to partnerships'};

const initials = name => name.split(' ').map(x=>x[0]).slice(0,2).join('');
const generatedCompanyLogo=company=>{
  const seed=[...company.name].reduce((sum,char)=>sum+char.charCodeAt(0),0),suffix=(company.name.match(/\d+$/)?.[0]||'').slice(-2),mark=`${initials(company.name)}${suffix}`;
  const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="160" height="160" viewBox="0 0 160 160"><rect width="160" height="160" rx="30" fill="#fff"/><rect x="7" y="7" width="146" height="146" rx="26" fill="${company.color}"/><circle cx="${36+seed%42}" cy="${31+seed%28}" r="${38+seed%17}" fill="#fff" opacity=".13"/><path d="M16 ${112-seed%20} L${92+seed%38} 15 L150 ${69+seed%35} V150 H16Z" fill="#071c54" opacity=".15"/><text x="80" y="93" text-anchor="middle" fill="#fff" font-family="Arial,sans-serif" font-size="${mark.length>3?38:48}" font-weight="800">${mark}</text><text x="80" y="126" text-anchor="middle" fill="#fff" opacity=".82" font-family="Arial,sans-serif" font-size="11" font-weight="700" letter-spacing="1">${company.industry.toUpperCase().slice(0,16)}</text></svg>`;
  return `data:image/svg+xml;charset=UTF-8,${encodeURIComponent(svg)}`;
};
companies.forEach(company=>{if(!company.logo)company.logo=generatedCompanyLogo(company)});
const companyLogo = (c,cls='logo') => `<div class="${cls}" role="img" aria-label="${c.name} logo" style="background:${c.color};background-image:url('${c.logo}')"></div>`;
const statusClass = s => s==='Investor Ready'?'ready':s==='Strong Potential'?'potential':'improve';

function navigate(page){
  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  document.getElementById(`page-${page}`)?.classList.add('active');
  document.querySelectorAll('.nav-item').forEach(n=>n.classList.toggle('active',n.dataset.page===page));
  document.getElementById('sidebar').classList.remove('open');
  window.scrollTo({top:0,behavior:'smooth'});
  if(location.hash.slice(1)!==page) history.replaceState(null,'',`#${page}`);
}
document.querySelectorAll('[data-page]').forEach(b=>b.addEventListener('click',()=>navigate(b.dataset.page)));
document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>navigate(b.dataset.go)));
document.getElementById('mobileMenu').onclick=()=>document.getElementById('sidebar').classList.toggle('open');

function renderDirectory(){
  const searchField=document.getElementById('companySearch');if(!searchField)return;
  const q=searchField.value.toLowerCase(), ind=document.getElementById('industryFilter').value, type=document.getElementById('typeFilter').value,membership=document.getElementById('directoryMembership')?.value||'all',verification=document.getElementById('directoryVerification')?.value||'all',minimumScore=Number(document.getElementById('directoryScore')?.value||0);
  const filtered=companies.filter(c=>(c.name+c.desc+c.tags.join(' ')).toLowerCase().includes(q)&&(ind==='all'||c.industry===ind)&&(type==='all'||c.type===type)&&(membership==='all'||c.membership===membership)&&(verification==='all'||(verification==='verified'&&isCompanyVerified(c))||(verification==='pending'&&!isCompanyVerified(c)))&&Number(c.score||0)>=minimumScore);
  document.getElementById('directoryGrid').innerHTML=filtered.map(c=>`<article class="business-card" data-company="${c.name}"><div class="business-top">${companyLogo(c)}<div><h3>${c.name} ${isCompanyVerified(c)?verifiedBadge():'<span class="status potential">Pending verification</span>'}</h3><p>${c.type} • ${c.industry}</p></div></div><p>${c.desc}</p><div class="tags">${c.tags.map(t=>`<span class="tag">${t}</span>`).join('')}</div><div class="business-bottom"><span>⌖ ${c.location}</span><span>View profile →</span></div></article>`).join('')||'<p>No companies match those filters.</p>';
  document.querySelectorAll('.business-card[data-company]').forEach(card=>card.onclick=()=>openCompanyProfile(card.dataset.company));
}
['companySearch','industryFilter','typeFilter','directoryMembership','directoryVerification','directoryScore'].forEach(id=>document.getElementById(id)?.addEventListener('input',renderDirectory));

let publicDirectoryView='grid';
let publicDirectoryPage=1;
let publicDirectoryExpanded=false;
let listingRequested=false;
function renderPublicDirectory(){
  const q=document.getElementById('publicCompanySearch').value.toLowerCase(),category=document.getElementById('publicCategoryFilter').value,membership=document.getElementById('publicMembershipFilter').value,sort=document.getElementById('publicDirectorySort').value,isFeaturedOnly=!publicDirectoryExpanded&&publicDirectoryView!=='logos',pageSize=publicDirectoryView==='logos'?200:(isFeaturedOnly?20:Number(document.getElementById('publicPageSize').value));
  const membershipOrder={VIP:0,Premium:1,Standard:2,Guest:3};
  const eligible=companies.filter(c=>(c.approved||currentMember?.email===c.ownerEmail)&&(c.name+c.desc+c.tags.join(' ')).toLowerCase().includes(q)&&(category==='all'||c.industry===category)&&(membership==='all'||c.membership===membership)).sort((a,b)=>sort==='name'?a.name.localeCompare(b.name):sort==='membership'?(membershipOrder[a.membership]-membershipOrder[b.membership])||a.name.localeCompare(b.name):(b.score||0)-(a.score||0));
  const pageCount=Math.max(1,Math.ceil(eligible.length/pageSize));
  publicDirectoryPage=Math.min(publicDirectoryPage,pageCount);
  const start=isFeaturedOnly?0:(publicDirectoryPage-1)*pageSize,visible=eligible.slice(start,start+pageSize);
  const section=document.getElementById('public-directory');
  section.dataset.directoryView=publicDirectoryView;
  section.classList.toggle('featured-directory-mode',isFeaturedOnly);
  logoViewNote.hidden=publicDirectoryView!=='logos';
  document.getElementById('publicPageSize').closest('label').style.display=publicDirectoryView==='logos'||isFeaturedOnly?'none':'';
  document.querySelectorAll('.directory-view-toggle [data-directory-view]').forEach(button=>button.classList.toggle('active',button.dataset.directoryView===publicDirectoryView));
  document.getElementById('publicCompanyGrid').innerHTML=visible.map((c,index)=>`<article class="public-company-card ${isFeaturedOnly&&index<4?'spotlight-company':''}"><div class="featured-company-cover" style="background-image:url('${c.cover||companyExtras[c.name]?.photos?.[0]||''}')"><span>${isFeaturedOnly?'Featured':'Directory'}</span></div><div class="public-company-head">${companyLogo(c)}<div><h3>${c.name} ${isCompanyVerified(c)?verifiedBadge():''}</h3><p>${c.type} • ${c.industry}</p></div><span class="public-membership ${c.membership.toLowerCase()}">${c.approved?c.membership:'Pending'}</span></div><p>${c.desc}</p><div class="tags">${c.tags.slice(0,3).map(t=>`<span class="tag">${t}</span>`).join('')}</div><div class="public-card-bottom"><span>⌖ ${c.location}</span><button class="public-profile" data-company="${c.name}">View profile →</button></div></article>`).join('')||'<p>No association businesses match these filters.</p>';
  document.querySelectorAll('.public-profile').forEach(b=>b.onclick=()=>openCompanyProfile(b.dataset.company));
  document.getElementById('publicDirectoryCount').textContent=eligible.length?(isFeaturedOnly?`Showing ${Math.min(20,eligible.length)} featured businesses from ${eligible.length.toLocaleString()} association businesses`:`Showing ${start+1}–${Math.min(start+pageSize,eligible.length)} of ${eligible.length.toLocaleString()} association businesses`):'No businesses found';
  const pages=[];for(let page=1;page<=pageCount;page++){if(page===1||page===pageCount||Math.abs(page-publicDirectoryPage)<=1)pages.push(page);else if(pages[pages.length-1]!=='…')pages.push('…')}
  document.getElementById('publicDirectoryPagination').innerHTML=isFeaturedOnly&&eligible.length>20?`<button class="view-all-businesses" data-public-expand="true">View all ${eligible.length.toLocaleString()} businesses →</button>`:`${!isFeaturedOnly&&eligible.length>20?'<button class="view-all-businesses secondary-view" data-public-expand="false">Show featured 20</button>':''}<button data-public-page="${publicDirectoryPage-1}" ${publicDirectoryPage===1?'disabled':''}>← Previous</button>${pages.map(page=>page==='…'?'<span>…</span>':`<button data-public-page="${page}" class="${page===publicDirectoryPage?'active':''}" aria-label="Page ${page}">${page}</button>`).join('')}<button data-public-page="${publicDirectoryPage+1}" ${publicDirectoryPage===pageCount?'disabled':''}>Next →</button>`;
}
['publicCompanySearch','publicCategoryFilter','publicMembershipFilter','publicDirectorySort','publicPageSize'].forEach(id=>document.getElementById(id).addEventListener('input',()=>{publicDirectoryPage=1;publicDirectoryExpanded=false;renderPublicDirectory()}));
const logoViewButton=document.createElement('button');
logoViewButton.dataset.directoryView='logos';
logoViewButton.title='Logo wall';
logoViewButton.setAttribute('aria-label','Logo wall');
logoViewButton.innerHTML='◉ <span>Logos</span>';
document.querySelector('.directory-view-toggle').append(logoViewButton);
logoViewButton.onclick=()=>{
  publicDirectoryView='logos';
  publicDirectoryPage=1;
  localStorage.setItem('idi-directory-view',publicDirectoryView);
  renderPublicDirectory();
};
const logoViewNote=document.createElement('div');
logoViewNote.className='logo-view-note';
logoViewNote.textContent='Logo wall shows every registered association company on one page. Use “View profile” to open its complete business information.';
logoViewNote.hidden=true;
document.getElementById('publicCompanyGrid').insertAdjacentElement('beforebegin',logoViewNote);
document.querySelector('.directory-view-toggle').onclick=e=>{const button=e.target.closest('[data-directory-view]');if(!button)return;publicDirectoryView=button.dataset.directoryView;localStorage.setItem('idi-directory-view',publicDirectoryView);renderPublicDirectory()};
document.getElementById('publicDirectoryPagination').onclick=e=>{const button=e.target.closest('[data-public-page]');if(!button||button.disabled)return;publicDirectoryPage=Number(button.dataset.publicPage);renderPublicDirectory();document.getElementById('public-directory').scrollIntoView({behavior:'smooth',block:'start'})};
document.getElementById('publicDirectoryPagination').addEventListener('click',e=>{const button=e.target.closest('[data-public-expand]');if(!button)return;publicDirectoryExpanded=button.dataset.publicExpand==='true';publicDirectoryPage=1;renderPublicDirectory();document.getElementById('public-directory').scrollIntoView({behavior:'smooth',block:'start'})});
const companyListingModal=document.getElementById('companyListingModal');
function openCompanyListing(){if(currentMember?.tier==='Guest'){showToast('Membership required','Choose Standard, Premium, or VIP membership to list a company.');return}document.getElementById('listingMembership').value=currentMember?.tier==='Admin'?'VIP':currentMember?.tier||'Standard';companyListingModal.classList.add('open')}
function closeCompanyListing(){companyListingModal.classList.remove('open')}
document.getElementById('listCompanyButton').onclick=openCompanyListing;
document.querySelectorAll('[data-company-close]').forEach(b=>b.onclick=closeCompanyListing);
const requiredLogoInput=document.querySelector('#companyListingForm [name="logo"]');
requiredLogoInput.required=true;
const requiredLogoLabel=requiredLogoInput.closest('label');
requiredLogoLabel.querySelector('span').innerHTML='Company logo <b class="logo-required-mark">*</b>';
requiredLogoLabel.querySelector('small').textContent='Required • square PNG, JPG, WebP, or SVG';
document.getElementById('companyListingForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),name=String(f.get('companyName')),tags=String(f.get('keywords')).split(',').map(x=>x.trim()).filter(Boolean),logoFile=f.get('logo'),coverFile=f.get('cover'),photoFiles=f.getAll('photos').filter(file=>file&&file.size),membership=currentMember?.tier&&currentMember.tier!=='Admin'?currentMember.tier:(f.get('membership')||'Standard'),ownerEmail=String(f.get('businessEmail'));const logo=logoFile&&logoFile.size?URL.createObjectURL(logoFile):'',cover=coverFile&&coverFile.size?URL.createObjectURL(coverFile):'';companies.push({name,industry:f.get('category'),type:'Member Company',membership,location:f.get('location'),desc:f.get('description'),tags:tags.length?tags:['Association Member'],color:'#5b4bdb',score:0,approved:false,ownerEmail,logo,cover});companyExtras[name]={...defaultExtra,website:f.get('website')||'Not provided',email:ownerEmail,phone:f.get('phone'),products:String(f.get('products')).split(/\n|,/).map(x=>x.trim()).filter(Boolean),services:String(f.get('services')).split(/\n|,/).map(x=>x.trim()).filter(Boolean),photos:photoFiles.map(file=>URL.createObjectURL(file)),social:{facebook:f.get('facebook'),linkedin:f.get('linkedin'),telegram:f.get('telegram'),instagram:f.get('instagram')}};const submissions=JSON.parse(localStorage.getItem('idi-company-submissions')||'[]');submissions.push({reference:`IDI-COMP-${String(Date.now()).slice(-6)}`,name,email:ownerEmail,membership,category:f.get('category'),location:f.get('location'),status:'Pending review',submittedAt:new Date().toISOString()});localStorage.setItem('idi-company-submissions',JSON.stringify(submissions));closeCompanyListing();renderPublicDirectory();renderDirectory();showToast('Company registration received','Your directory profile is pending association verification.');e.target.reset()};
const companySocialSection=document.querySelectorAll('#companyListingForm .editor-section')[2];
companySocialSection.insertAdjacentHTML('beforebegin',`<div class="editor-section company-verification-section"><h3>Company verification</h3><p class="company-verification-note">Company registration does not include a funding request. Upload the legal information below so IDI can review the company before publishing a verified directory profile.</p><div class="form-row"><label>Company registration number<input name="registrationNumber" required placeholder="Registration / incorporation number" /></label><label>Incorporation date<input name="incorporationDate" type="date" required /></label></div><div class="form-row"><label>Legal representative<input name="legalRepresentative" required placeholder="Full name and official position" /></label><label>Company size<select name="companySize"><option>1–9 employees</option><option>10–49 employees</option><option>50–249 employees</option><option>250+ employees</option></select></label></div><div class="readiness-upload-grid"><label>Registration certificate<input type="file" name="registrationCertificate" accept=".pdf,.jpg,.jpeg,.png" required /><small>Official incorporation certificate</small></label><label>Tax patent / tax registration<input type="file" name="taxPatent" accept=".pdf,.jpg,.jpeg,.png" required /><small>Current tax registration evidence</small></label><label>Representative ID / authority<input type="file" name="representativeId" accept=".pdf,.jpg,.jpeg,.png" required /><small>ID and proof of authority</small></label><label>Operating license (if applicable)<input type="file" name="operatingLicense" accept=".pdf,.jpg,.jpeg,.png" /><small>Sector or professional license</small></label></div><p class="company-verification-note">Verification is granted only after the profile is complete, documents are approved, membership payment is confirmed, and an IDI administrator gives final approval.</p></div>`);
const baseCompanyListingSubmit=document.getElementById('companyListingForm').onsubmit;
document.getElementById('companyListingForm').onsubmit=e=>{const form=new FormData(e.target),name=String(form.get('companyName')),verification={registrationNumber:form.get('registrationNumber'),incorporationDate:form.get('incorporationDate'),legalRepresentative:form.get('legalRepresentative'),companySize:form.get('companySize'),informationStatus:'Complete',documentStatus:'Under review',paymentStatus:'Pending confirmation',reviewStatus:'Pending',verificationStatus:'Unverified',documents:{registrationCertificate:form.get('registrationCertificate')?.name||'',taxPatent:form.get('taxPatent')?.name||'',representativeId:form.get('representativeId')?.name||'',operatingLicense:form.get('operatingLicense')?.name||''}};baseCompanyListingSubmit(e);Object.assign(companyExtras[name]||{}, {registration:verification.registrationNumber,employees:verification.companySize,verification});const submissions=JSON.parse(localStorage.getItem('idi-company-submissions')||'[]');if(submissions.length){Object.assign(submissions[submissions.length-1],verification);localStorage.setItem('idi-company-submissions',JSON.stringify(submissions))}renderCompanyVerification()};
const companyProfileModal=document.getElementById('companyProfileModal');
function openCompanyProfile(name){const c=companies.find(x=>x.name===name);if(!c)return;const x=companyExtras[name]||defaultExtra;document.getElementById('companyProfileContent').innerHTML=`<div class="profile-cover"></div><div class="profile-identity"><div class="profile-logo" style="background:${c.color}">${initials(c.name)}</div><div><h2>${c.name} <span class="verified">●</span></h2><p>${c.type} • ${c.industry} • ${c.location}</p></div><span class="public-membership ${c.membership.toLowerCase()}">${c.membership} member</span></div><div class="profile-body"><div><section class="profile-section"><h3>Company overview</h3><p>${c.desc}</p><div class="detail-grid"><div><span>Founded</span><strong>${x.founded}</strong></div><div><span>Team size</span><strong>${x.employees}</strong></div><div><span>Registration</span><strong>${x.registration}</strong></div></div></section><section class="profile-section"><h3>Products</h3><div class="offering-grid">${x.products.map(v=>`<div class="offering"><strong>${v}</strong><span>Product offering</span></div>`).join('')}</div></section><section class="profile-section"><h3>Services</h3><div class="offering-grid">${x.services.map(v=>`<div class="offering"><strong>${v}</strong><span>Professional service</span></div>`).join('')}</div></section><section class="profile-section"><h3>Markets & expertise</h3><div class="tags">${[...c.tags,x.markets].map(v=>`<span class="tag">${v}</span>`).join('')}</div></section></div><aside class="profile-side"><h3>Company information</h3><div class="profile-fact"><span>Website</span><strong>${x.website}</strong></div><div class="profile-fact"><span>Email</span><strong>${x.email}</strong></div><div class="profile-fact"><span>Phone</span><strong>${x.phone}</strong></div><div class="profile-fact"><span>Business stage</span><strong>${x.stage}</strong></div><div class="profile-fact"><span>Annual revenue</span><strong>${x.revenue}</strong></div><div class="profile-fact"><span>Investment status</span><strong>${x.funding}</strong></div><button class="primary profile-contact">Request introduction</button><button class="secondary profile-save" style="width:100%;margin-top:8px">Save company</button></aside></div>`;companyProfileModal.classList.add('open');companyProfileModal.querySelector('.profile-contact').onclick=()=>showToast('Introduction requested',`${c.name} will receive your request.`);companyProfileModal.querySelector('.profile-save').onclick=()=>showToast('Company saved',`${c.name} was added to your saved companies.`)}
function closeCompanyProfile(){companyProfileModal.classList.remove('open')}
document.querySelectorAll('[data-profile-close]').forEach(b=>b.onclick=closeCompanyProfile);
function openCompanyProfile(name){const c=companies.find(x=>x.name===name);if(!c)return;const x=companyExtras[name]||defaultExtra,photos=x.photos||[],social=x.social||{};document.getElementById('companyProfileContent').innerHTML=`<div class="profile-cover ${c.cover?'has-image':''}" ${c.cover?`style="background-image:url('${c.cover}')"`:''}></div><div class="profile-identity"><div class="profile-logo ${c.logo?'has-image':''}" style="background:${c.color}${c.logo?`;background-image:url('${c.logo}')`:''}">${c.logo?'':initials(c.name)}</div><div><h2>${c.name} ${c.approved?'<span class="verified">●</span>':'<span class="status potential">Pending verification</span>'}</h2><p>${c.type} • ${c.industry} • ${c.location}</p></div><span class="public-membership ${c.membership.toLowerCase()}">${c.membership} member</span></div><div class="profile-body"><div><section class="profile-section"><h3>Company overview</h3><p>${c.desc}</p><div class="detail-grid"><div><span>Founded</span><strong>${x.founded}</strong></div><div><span>Team size</span><strong>${x.employees}</strong></div><div><span>Registration</span><strong>${x.registration}</strong></div></div></section><section class="profile-section"><h3>Products</h3><div class="offering-grid">${x.products.map(v=>`<div class="offering"><strong>${v}</strong><span>Product offering</span></div>`).join('')}</div></section><section class="profile-section"><h3>Services</h3><div class="offering-grid">${x.services.map(v=>`<div class="offering"><strong>${v}</strong><span>Professional service</span></div>`).join('')}</div></section><section class="profile-section"><h3>Business photo gallery</h3><div class="company-gallery">${photos.length?photos.map((url,i)=>`<div class="gallery-photo" style="background-image:url('${url}')" aria-label="Business photo ${i+1}"></div>`).join(''):`<div class="gallery-photo">Product photo</div><div class="gallery-photo">Team photo</div><div class="gallery-photo">Office / project</div>`}</div></section><section class="profile-section"><h3>Markets & expertise</h3><div class="tags">${[...c.tags,x.markets].map(v=>`<span class="tag">${v}</span>`).join('')}</div></section></div><aside class="profile-side"><h3>Company information</h3><div class="profile-fact"><span>Website</span><strong>${x.website}</strong></div><div class="profile-fact"><span>Email</span><strong>${x.email}</strong></div><div class="profile-fact"><span>Phone</span><strong>${x.phone}</strong></div><div class="profile-fact"><span>Business stage</span><strong>${x.stage}</strong></div><div class="profile-fact"><span>Annual revenue</span><strong>${x.revenue}</strong></div><div class="profile-fact"><span>Investment status</span><strong>${x.funding}</strong></div><h3 style="margin-top:16px">Social media</h3><div class="social-links">${[['Facebook',social.facebook],['LinkedIn',social.linkedin],['Telegram',social.telegram],['Instagram',social.instagram]].map(s=>`<a class="social-link ${s[1]?'':'disabled'}" ${s[1]?`href="${s[1]}" target="_blank"`:''}>${s[0]}</a>`).join('')}</div><button class="primary profile-contact">Request introduction</button><button class="secondary profile-save" style="width:100%;margin-top:8px">Save company</button></aside></div>`;companyProfileModal.classList.add('open');companyProfileModal.querySelector('.profile-contact').onclick=()=>showToast('Introduction requested',`${c.name} will receive your request.`);companyProfileModal.querySelector('.profile-save').onclick=()=>showToast('Company saved',`${c.name} was added to your saved companies.`)}

const openCompanyProfileBase=openCompanyProfile;
openCompanyProfile=function(name){
  openCompanyProfileBase(name);
  const company=companies.find(item=>item.name===name),heading=document.querySelector('#companyProfileContent .profile-identity h2');
  if(!company||!heading)return;
  heading.querySelector('.verified,.verified-badge,.status')?.remove();
  heading.insertAdjacentHTML('beforeend',isCompanyVerified(company)?` ${verifiedBadge()}`:' <span class="status potential">Pending verification</span>');
};

let fundingRequests=JSON.parse(localStorage.getItem('idi-funding-requests')||'[]');
if(!fundingRequests.length)fundingRequests=[{reference:'IDI-FUND-260628',company:'Mekong AgriTech',type:'Equity investment',amount:'$1,200,000',submitted:'28 Jun 2026',status:'AI Assessment',nextAction:'Complete readiness assessment'}];
const companyVerificationRecords=[
  {company:'Mekong AgriTech',information:'Complete',documents:'Approved',payment:'Paid',review:'Approved',verified:true},
  {company:'BrightPath Academy',information:'Complete',documents:'Under review',payment:'Paid',review:'Pending',verified:false},
  {company:'New SME Applicant',information:'Incomplete',documents:'Missing',payment:'Pending',review:'Not started',verified:false}
];
function fundingEligible(profile=currentMember){return !!profile&&!profile.isAdmin&&['VIP','Premium'].includes(profile.tier)&&profile.paymentStatus==='Paid'&&profile.verified===true}
function renderFundingAccess(profile=currentMember){const form=document.getElementById('fundingRequestForm');if(!form)return;const eligible=fundingEligible(profile),admin=!!profile?.isAdmin,title=document.getElementById('fundingEligibilityTitle'),text=document.getElementById('fundingEligibilityText'),badge=document.getElementById('fundingEligibilityBadge');if(admin){title.textContent='Administrator review workspace';text.textContent='Review submitted funding requests and monitor their progress through the investment workflow.';badge.textContent='ADMIN REVIEW'}else if(eligible){title.textContent='Eligible verified member';text.textContent='Your paid membership and verified company allow you to submit a funding request.';badge.textContent=`${profile.tier.toUpperCase()} ACCESS`}else{title.textContent='Funding request access locked';text.textContent='This tool requires an active paid Premium or VIP membership and an IDI-verified company.';badge.textContent='NOT ELIGIBLE'}form.classList.toggle('funding-tier-lock',!eligible);form.querySelectorAll('input,select,textarea,button').forEach(control=>control.disabled=!eligible)}
function renderFundingRequests(){const table=document.getElementById('fundingRequestTable');if(!table)return;table.innerHTML=fundingRequests.map(request=>`<tr><td><strong>${request.reference}</strong></td><td>${request.company}</td><td>${request.type}</td><td>${request.amount}</td><td>${request.submitted}</td><td><span class="request-status">${request.status}</span></td><td>${request.nextAction}</td></tr>`).join('')||'<tr><td colspan="7">No funding requests submitted.</td></tr>'}
document.getElementById('fundingRequestForm').onsubmit=e=>{e.preventDefault();if(!fundingEligible()){showToast('Funding request unavailable','A paid, verified Premium or VIP membership is required.');return}const form=new FormData(e.target),reference=`IDI-FUND-${String(Date.now()).slice(-6)}`;fundingRequests.unshift({reference,company:form.get('company'),type:form.get('fundingType'),amount:form.get('amount'),submitted:new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(new Date()),status:'Submitted',nextAction:'IDI completeness review'});localStorage.setItem('idi-funding-requests',JSON.stringify(fundingRequests));renderFundingRequests();e.target.reset();showToast('Funding request submitted',`${reference} is ready for IDI completeness review.`)};
document.getElementById('fundingGuidance').onclick=()=>showToast('Funding preparation guide','Prepare a pitch deck, financial model, capitalization table, funding purpose, and detailed use of funds.');
function verificationClass(value){return /^(Complete|Approved|Paid|Verified)$/.test(value)?'approved':/^(Under review|Pending)$/.test(value)?'pending':'blocked'}
function renderCompanyVerification(){const table=document.getElementById('companyVerificationTable');if(!table)return;const saved=JSON.parse(localStorage.getItem('idi-company-submissions')||'[]').map(record=>({company:record.name,information:record.informationStatus||'Complete',documents:record.documentStatus||'Under review',payment:record.paymentStatus||'Pending',review:record.reviewStatus||'Pending',verified:record.verificationStatus==='Verified'}));const rows=[...saved,...companyVerificationRecords];table.innerHTML=rows.map((record,index)=>{const eligible=record.information==='Complete'&&record.documents==='Approved'&&record.payment==='Paid';return `<tr><td><strong>${record.company}</strong></td><td><span class="verification-state ${verificationClass(record.information)}">${record.information}</span></td><td><span class="verification-state ${verificationClass(record.documents)}">${record.documents}</span></td><td><span class="verification-state ${verificationClass(record.payment)}">${record.payment}</span></td><td><span class="verification-state ${verificationClass(record.review)}">${record.review}</span></td><td><span class="verification-state ${record.verified?'approved':'blocked'}">${record.verified?'Verified':'Unverified'}</span></td><td><button class="secondary verify-company" data-verification-index="${index}" ${!eligible||record.verified?'disabled':''}>${record.verified?'Approved':'Approve'}</button></td></tr>`}).join('');table.querySelectorAll('.verify-company:not(:disabled)').forEach(button=>button.onclick=()=>{const record=rows[Number(button.dataset.verificationIndex)];record.review='Approved';record.verified=true;const company=companies.find(item=>item.name===record.company);if(company)Object.assign(company,{approved:true,verified:true,profileComplete:true,documentsVerified:true,paymentVerified:true,adminVerified:true});renderCompanyVerification();renderDirectory();renderPublicDirectory();showToast('Company verified',`${record.company} now displays the IDI verified blue tick.`)})}

let activePitchFilter='all';
function renderPitches(){
  const q=document.getElementById('pitchSearch').value.toLowerCase();
  const rows=pitches.filter(p=>(activePitchFilter==='all'||p.status===activePitchFilter)&&p.name.toLowerCase().includes(q));
  document.getElementById('pitchTable').innerHTML=rows.map(p=>`<tr><td><div class="table-company"><div class="logo" style="background:${p.color}">${initials(p.name)}</div><div><strong>${p.name}</strong><span>${p.stage}</span></div></div></td><td>${p.industry}</td><td><div class="mini-progress"><i style="--w:${p.complete}%"></i>${p.complete}%</div></td><td><strong>${p.score}</strong> / 100</td><td><span class="status ${statusClass(p.status)}">${p.status}</span></td><td>${p.date}</td><td><button class="row-action">⋯</button></td></tr>`).join('');
}
document.getElementById('pitchTabs').addEventListener('click',e=>{if(!e.target.dataset.filter)return;activePitchFilter=e.target.dataset.filter;document.querySelectorAll('#pitchTabs button').forEach(b=>b.classList.toggle('active',b===e.target));renderPitches()});
document.getElementById('pitchSearch').addEventListener('input',renderPitches);

let activeEventFilter='upcoming';
function renderEvents(){
  const saved=JSON.parse(localStorage.getItem('uni-rsvps')||'{}');
  const rows=events.filter(event=>(event.status||'upcoming').toLowerCase()===activeEventFilter);
  document.getElementById('eventsGrid').innerHTML=rows.map(e=>{const i=events.indexOf(e);return `<article class="event-card"><div class="event-cover ${e.theme||''}"><span class="badge">${e.type}</span><div class="date"><strong>${e.day}</strong> <span>${e.month}</span></div></div><div class="event-body"><h3>${e.title}</h3><p>⌖ ${e.place}</p><div class="invite-stats"><div><strong>${e.accepted+(saved[i]?1:0)}</strong><span>ACCEPTED</span></div><div><strong>${e.maybe}</strong><span>MAYBE</span></div><div><strong>${Math.max(0,e.pending-(saved[i]?1:0))}</strong><span>PENDING</span></div></div><div class="event-actions"><button class="secondary event-manage" data-event-manage="${i}">Manage</button><button class="primary rsvp-btn" data-index="${i}">${saved[i]?'✓ Accepted':'Send invitation'}</button></div></div></article>`}).join('')||`<div class="page-complete-note"><span>✓</span><strong>No ${activeEventFilter} events. Create one or change the filter.</strong></div>`;
  document.querySelectorAll('.rsvp-btn').forEach(b=>b.onclick=()=>{const s=JSON.parse(localStorage.getItem('uni-rsvps')||'{}');s[b.dataset.index]=true;localStorage.setItem('uni-rsvps',JSON.stringify(s));renderEvents();showToast('Invitation accepted','The RSVP and attendance list were updated.')});
  document.querySelectorAll('[data-event-manage]').forEach(button=>button.onclick=()=>{const event=events[Number(button.dataset.eventManage)];showToast('Event management',`${event.title}: edit details, invitations, registrations, check-in, and attendance reporting.`)});
}
document.getElementById('eventTabs').onclick=e=>{const button=e.target.closest('[data-event-filter]');if(!button)return;activeEventFilter=button.dataset.eventFilter;document.querySelectorAll('#eventTabs button').forEach(item=>item.classList.toggle('active',item===button));renderEvents()};
const eventEditorModal=document.getElementById('eventEditorModal');
function openEventEditor(){eventEditorModal.classList.add('open');eventEditorModal.setAttribute('aria-hidden','false')}
function closeEventEditor(){eventEditorModal.classList.remove('open');eventEditorModal.setAttribute('aria-hidden','true')}
document.getElementById('createEvent').onclick=openEventEditor;
document.querySelectorAll('[data-event-editor-close]').forEach(button=>button.onclick=closeEventEditor);
document.getElementById('eventEditorForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),start=new Date(form.get('start')),status=String(form.get('status')).toLowerCase();events.unshift({day:String(start.getDate()).padStart(2,'0'),month:start.toLocaleString('en',{month:'short'}).toUpperCase(),type:form.get('type'),title:form.get('title'),place:`${form.get('venue')} • ${start.toLocaleTimeString('en',{hour:'numeric',minute:'2-digit'})}`,description:form.get('description'),accepted:0,maybe:0,pending:0,theme:'green-cover',public:form.getAll('audience').includes('Public'),capacity:Number(form.get('capacity')),status});activeEventFilter=status;document.querySelectorAll('#eventTabs button').forEach(item=>item.classList.toggle('active',item.dataset.eventFilter===status));closeEventEditor();renderEvents();renderPublicEvents();renderHomeHighlights();showToast('Event created',`${form.get('title')} is saved as ${form.get('status')}.`);e.target.reset()};
const calendarModal=document.getElementById('calendarModal');
function renderEventCalendar(){const occupied=new Map(events.map(event=>[Number(event.day),event.title]));document.getElementById('eventCalendarBoard').innerHTML=Array.from({length:31},(_,index)=>{const day=index+1,title=occupied.get(day);return `<div class="calendar-day ${title?'has-event':''}"><b>${day}</b>${title?`<span>${title}</span>`:''}</div>`}).join('')}
document.getElementById('calendarView').onclick=()=>{renderEventCalendar();calendarModal.classList.add('open')};
document.querySelectorAll('[data-calendar-close]').forEach(button=>button.onclick=()=>calendarModal.classList.remove('open'));

function renderPublicEvents(){
  const grid=document.getElementById('publicEventsGrid');if(!grid)return;
  const monthMap={JAN:0,FEB:1,MAR:2,APR:3,MAY:4,JUN:5,JUL:6,AUG:7,SEP:8,OCT:9,NOV:10,DEC:11},monthNames=['January','February','March','April','May','June','July','August','September','October','November','December'];
  const visibleEvents=events.filter(event=>(event.status||'upcoming')==='upcoming'&&(event.public||event.accessType==='activity')).sort((a,b)=>new Date(a.date||`2026-${String((monthMap[a.month]||6)+1).padStart(2,'0')}-${a.day}`)-new Date(b.date||`2026-${String((monthMap[b.month]||6)+1).padStart(2,'0')}-${b.day}`));
  renderPublicEvents.state=renderPublicEvents.state||{month:6,week:'all',selectedDay:null};
  const state=renderPublicEvents.state,months=[...new Set(visibleEvents.map(event=>monthMap[event.month]??6))].sort((a,b)=>a-b);
  if(!months.includes(state.month))state.month=months[0]??6;
  const monthIndex=state.month,monthName=`${monthNames[monthIndex]} 2026`,daysInMonth=new Date(2026,monthIndex+1,0).getDate(),leadingDays=(new Date(2026,monthIndex,1).getDay()+6)%7;
  const monthEvents=visibleEvents.filter(event=>(monthMap[event.month]??6)===monthIndex);
  const weekOfDay=day=>Math.ceil((leadingDays+Number(day))/7);
  const filteredEvents=monthEvents.filter(event=>state.week==='all'||weekOfDay(event.day)===Number(state.week));
  const selectedItems=state.selectedDay?monthEvents.filter(event=>event.day===state.selectedDay):filteredEvents.slice(0,1);
  const summaryItems=(selectedItems.length?selectedItems:filteredEvents.slice(0,1));
  const dayCells=[...Array(leadingDays)].map(()=>'<div class="public-calendar-day muted"></div>').concat(Array.from({length:daysInMonth},(_,i)=>{const day=String(i+1).padStart(2,'0'),items=monthEvents.filter(event=>event.day===day),dimmed=state.week!=='all'&&weekOfDay(day)!==Number(state.week);return `<button class="public-calendar-day ${items.length?'has-event':''} ${state.selectedDay===day?'selected':''} ${dimmed?'dimmed':''}" ${items.length?`data-calendar-day="${day}" data-summary-events="${items.map(event=>events.indexOf(event)).join(',')}"`:''}><b>${i+1}</b>${items.slice(0,2).map(event=>`<span class="${event.public?'public-dot':'activity-dot'}">${event.public?'Event':'Activity'}</span>`).join('')}${items.length>2?`<em>+${items.length-2}</em>`:''}</button>`})).join('');
  const weekButtons=['all',1,2,3,4,5].map(week=>`<button class="${state.week===week?'active':''}" data-public-week="${week}">${week==='all'?'All weeks':`Week ${week}`}</button>`).join('');
  const summary=summaryItems.map(event=>{const index=events.indexOf(event),activity=event.accessType==='activity',remaining=Math.max(0,(event.capacity||0)-(event.accepted||0));return `<article><span>${activity?'IDI Activity':event.type}</span><h4>${event.title}</h4><p>${event.description}</p><div><b>${event.day} ${event.month}</b><b>${event.place}</b>${event.public?`<b>${remaining} seats</b>`:`<b>${event.role||'IDI participation'}</b>`}</div>${event.public?`<button class="primary public-event-register" data-public-event="${index}">Register / detail</button>`:`<button class="secondary public-activity-detail" data-public-activity="${index}">View detail</button>`}</article>`}).join('');
  const roadmap=filteredEvents.map((event,position)=>{const index=events.indexOf(event),activity=event.accessType==='activity',remaining=Math.max(0,(event.capacity||0)-(event.accepted||0));return `<article class="public-roadmap-card ${activity?'activity':'event'}"><div class="roadmap-node">${String(position+1).padStart(2,'0')}</div><div class="roadmap-date"><strong>${event.day}</strong><span>${event.month}</span></div><small>${activity?'IDI activity':event.type}</small><h3>${event.title}</h3><div class="roadmap-meta"><span>${event.place.split('•')[0].trim()}</span>${event.public?`<b>${remaining} seats</b>`:`<b>${event.role||'IDI participation'}</b>`}</div>${event.public?`<button class="primary public-event-register" data-public-event="${index}">Register</button>`:`<button class="secondary public-activity-detail" data-public-activity="${index}">Detail</button>`}</article>`}).join('');
  grid.innerHTML=`<div class="public-calendar-shell professional-schedule"><article class="public-calendar-panel"><div class="public-calendar-top"><div><span>IDI SCHEDULE</span><h3>${monthName}</h3><p>Compact public calendar for events and activities.</p></div><div class="public-calendar-actions"><button data-public-month="-1">‹</button><button data-public-month="1">›</button></div></div><div class="public-week-filter">${weekButtons}</div><div class="public-calendar-legend"><span><i></i>Public event</span><span><i></i>IDI activity</span></div><div class="public-calendar-weekdays"><span>Mon</span><span>Tue</span><span>Wed</span><span>Thu</span><span>Fri</span><span>Sat</span><span>Sun</span></div><div class="public-calendar-board">${dayCells}</div></article><aside class="public-day-summary"><div class="public-day-summary-head"><span>DAY SUMMARY</span><strong>${state.selectedDay?`${state.selectedDay} ${monthNames[monthIndex].slice(0,3).toUpperCase()}`:'Highlighted activity'}</strong></div><div class="public-day-summary-body">${summary||'<p>Select an event day to see summary and detail link.</p>'}</div></aside><section class="public-roadmap-panel"><div class="public-roadmap-head"><div><span>EVENT ROADMAP</span><strong>Upcoming → ${filteredEvents.length} public events & IDI activities</strong><p>Scroll left or right to view the next public programs and IDI activities.</p></div></div><div class="public-roadmap-track">${roadmap||'<p class="public-roadmap-empty">No activities for this week. Choose another week or month.</p>'}</div></section></div>`;
  document.querySelectorAll('[data-public-month]').forEach(button=>button.onclick=()=>{const currentIndex=months.indexOf(renderPublicEvents.state.month),nextIndex=Math.min(Math.max(currentIndex+Number(button.dataset.publicMonth),0),months.length-1);renderPublicEvents.state.month=months[nextIndex]??renderPublicEvents.state.month;renderPublicEvents.state.week='all';renderPublicEvents.state.selectedDay=null;renderPublicEvents()});
  document.querySelectorAll('[data-public-week]').forEach(button=>button.onclick=()=>{renderPublicEvents.state.week=button.dataset.publicWeek==='all'?'all':Number(button.dataset.publicWeek);renderPublicEvents.state.selectedDay=null;renderPublicEvents()});
  document.querySelectorAll('[data-calendar-day]').forEach(day=>{const selectDay=()=>{renderPublicEvents.state.selectedDay=day.dataset.calendarDay;renderPublicEvents()};day.onclick=selectDay;day.onmouseenter=selectDay});
  document.querySelectorAll('[data-public-event]').forEach(button=>button.onclick=()=>openPublicEventRegistration(Number(button.dataset.publicEvent)));
  document.querySelectorAll('[data-public-activity]').forEach(button=>button.onclick=()=>typeof openEventBrief==='function'?openEventBrief(Number(button.dataset.publicActivity)):showToast('IDI activity',events[Number(button.dataset.publicActivity)]?.description||'Activity detail'));
}

function renderHomeHighlights(){
  const newsFeature=document.getElementById('homeHotNewsFeature'),newsList=document.getElementById('homeHotNewsList'),eventFeature=document.getElementById('homeUpcomingEventFeature'),eventList=document.getElementById('homeUpcomingEventList');
  if(!newsFeature||!newsList||!eventFeature||!eventList)return;
  const publicNews=newsItems.filter(item=>item.audiences.includes('Guest')&&new Date(item.publishAt)<=new Date()).sort((a,b)=>new Date(b.publishAt)-new Date(a.publishAt)).slice(0,3);
  const leadNews=publicNews[0];
  newsFeature.innerHTML=leadNews?`<article class="priority-news-main" data-home-news="${newsItems.indexOf(leadNews)}"><div class="priority-visual ${leadNews.thumbnail||leadNews.heroImage?'has-image':''}" ${leadNews.thumbnail||leadNews.heroImage?`style="background-image:url('${leadNews.thumbnail||leadNews.heroImage}')"`:''}><span>${leadNews.sourceType||'Hot news'}</span></div><div class="latest-news-ribbon"><b>Latest update</b><span>${formatNewsDate(leadNews.publishAt)}</span></div><div class="priority-copy"><small><i></i>${leadNews.category||'Financial News'}</small><h3>${leadNews.headline}</h3><p>${leadNews.tagline}</p><div class="priority-meta"><b>${leadNews.audiences.length===4?'Public news':leadNews.audiences.join(', ')}</b><span>${leadNews.author||leadNews.source}</span><em>Read full update →</em></div></div></article>`:'<div class="priority-empty">Latest public financial news will appear here.</div>';
  newsList.innerHTML=publicNews.slice(1,3).map((item,index)=>`<button class="priority-mini financial-news-mini" data-home-news="${newsItems.indexOf(item)}"><span class="priority-mini-icon">${index===0?'NEW':'FN'}</span><span><strong>${item.headline}</strong><span>${formatNewsDate(item.publishAt)} • ${item.category||item.sourceType}</span></span><em>Read</em></button>`).join('');
  document.querySelectorAll('[data-home-news]').forEach(item=>item.onclick=()=>openNewsArticle(Number(item.dataset.homeNews)));
  const upcoming=events.filter(event=>event.public&&(event.status||'upcoming')==='upcoming').slice(0,3);
  const leadEvent=upcoming[0];
  eventFeature.innerHTML=leadEvent?`<article class="priority-event-main"><div class="priority-event-date"><div><span>${leadEvent.month}</span><strong>${leadEvent.day}</strong><em>2026</em></div></div><div class="priority-copy"><small>Latest & upcoming event</small><h3>${leadEvent.title}</h3><p>${leadEvent.description}</p><div class="event-brief-row"><span>${leadEvent.type}</span><span>⌖ ${leadEvent.place}</span><span>◷ ${leadEvent.time||'09:00 AM'}</span><b>${Math.max(0,leadEvent.capacity-leadEvent.accepted)} seats available</b></div><button class="primary public-event-register" data-home-event="${events.indexOf(leadEvent)}">Register now</button></div></article>`:'<div class="priority-empty">Upcoming public events will appear here.</div>';
  eventList.innerHTML=upcoming.slice(1,3).map(event=>{const index=events.indexOf(event),remaining=Math.max(0,event.capacity-event.accepted);return `<article class="priority-mini event-agenda-mini"><span class="priority-mini-icon"><b>${event.day}</b><small>${event.month}</small></span><span class="event-mini-copy"><strong>${event.title}</strong><span>${event.type} • ${event.place}</span><small>${event.description}</small></span><span class="event-mini-actions"><b>${remaining} seats</b><button class="primary public-event-register" data-home-event="${index}">Register</button></span></article>`}).join('');
  document.querySelectorAll('[data-home-event]').forEach(item=>item.onclick=()=>openPublicEventRegistration(Number(item.dataset.homeEvent)));
}

const publicEventModal=document.getElementById('publicEventModal');
function openPublicEventRegistration(index){const event=events[index];if(!event)return;publicEventModal.querySelector('[name="eventIndex"]').value=index;document.getElementById('publicEventTitle').textContent=event.title;document.getElementById('publicEventMeta').textContent=`${event.day} ${event.month} 2026 • ${event.place}`;publicEventModal.classList.add('open');publicEventModal.setAttribute('aria-hidden','false')}
function closePublicEventRegistration(){publicEventModal.classList.remove('open');publicEventModal.setAttribute('aria-hidden','true')}
document.querySelectorAll('[data-public-event-close]').forEach(button=>button.onclick=closePublicEventRegistration);
publicEventModal.onclick=e=>{if(e.target===publicEventModal)closePublicEventRegistration()};
document.getElementById('publicEventForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),index=Number(form.get('eventIndex')),event=events[index],seats=Number(form.get('seats'));const registrations=JSON.parse(localStorage.getItem('uni-public-event-registrations')||'[]');registrations.push({event:event.title,name:form.get('fullName'),email:form.get('email'),phone:form.get('phone'),organization:form.get('organization'),attendeeType:form.get('attendeeType'),seats,registeredAt:new Date().toISOString()});localStorage.setItem('uni-public-event-registrations',JSON.stringify(registrations));event.accepted+=seats;closePublicEventRegistration();renderPublicEvents();renderEvents();renderHomeHighlights();showToast('Registration confirmed',`You are registered for ${event.title}. A confirmation will be sent to ${form.get('email')}.`);e.target.reset()};

let activeNewsFilter='all';
function renderAdminNews(){
  const rows=newsItems.filter(n=>(!currentMember||currentMember.canPublish||n.audiences.includes(currentMember.tier))&&(activeNewsFilter==='all'||n.audiences.includes(activeNewsFilter)||n.sourceType===activeNewsFilter));
  document.getElementById('newsAdminList').innerHTML=rows.map(n=>{const index=newsItems.indexOf(n);return `<div class="news-admin-item professional-news"><button class="news-feed-thumb ${n.thumbnail?'has-image':''}" data-news-index="${index}" ${n.thumbnail?`style="background-image:url('${n.thumbnail}')"`:''}><span>${n.category?.split(' ')[0]||'NEWS'}</span></button><div class="news-title-row"><div><span class="news-kicker">${n.category||n.sourceType}</span><h3>${n.headline}</h3><p>${n.tagline||`${n.source} • ${n.sourceType}`}</p><div class="news-meta-line"><span>By ${n.author||n.source}</span><span>${formatNewsDate(n.publishAt)}</span></div><div class="delivery-badges">${n.channels.map(c=>`<i>${c==='app'?'♢ App':c==='email'?'✉ Email':'◌ SMS'}</i>`).join('')}</div></div></div><span class="news-level l${n.level}">LEVEL ${n.level}<br>${n.level===3?'Restricted':n.level===2?'Strategic':'Public'}</span><div class="release-time"><strong>${n.release}</strong><span>${n.audiences.length===4?'All memberships':n.audiences.join(', ')}</span><button class="read-story" data-news-index="${index}">Read article →</button></div><span class="audience-badge">${n.audiences.length===4?'All':n.audiences.length+' tiers'}</span></div>`}).join('');
  document.querySelectorAll('[data-news-index]').forEach(button=>button.onclick=()=>openNewsArticle(Number(button.dataset.newsIndex)));
  renderPublicNews();
}

function renderPublicNews(){
  const publicNews=newsItems.filter(item=>item.audiences.includes('Guest')&&new Date(item.publishAt)<=new Date()).slice(0,3);
  const grid=document.getElementById('publicNewsGrid');grid.innerHTML=publicNews.map((item,i)=>{const index=newsItems.indexOf(item),image=item.thumbnail||item.heroImage;return `<article class="public-news-card ${i===0?'featured':''}" data-public-news="${index}"><div class="public-news-image" ${image?`style="background-image:url('${image}')"`:''}><span>${item.category||'Investment News'}</span></div><div class="public-news-copy"><h3>${item.headline}</h3><p>${item.tagline}</p><div class="public-news-footer"><span>${formatNewsDate(item.publishAt)} • ${item.author}</span><div class="public-news-actions"><button class="listen-news-btn" data-listen-news="${index}">▶ Listen</button><button data-public-news="${index}">Read story →</button></div></div></div></article>`}).join('')||'<div class="public-empty-news"><h3>Public news is coming soon</h3><p>New Guest-accessible investment updates will appear here after publication.</p></div>';
  document.querySelectorAll('[data-public-news]').forEach(card=>card.onclick=()=>openNewsArticle(Number(card.dataset.publicNews)));
  document.querySelectorAll('[data-listen-news]').forEach(button=>button.onclick=event=>{event.stopPropagation();startNewsAudio(Number(button.dataset.listenNews))});
  renderHomeHighlights();
}

function formatNewsDate(value){if(!value)return 'Just now';return new Intl.DateTimeFormat('en-GB',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value))}
function newsParagraphs(value){return String(value||'').split(/\n\s*\n/).filter(Boolean).map(p=>`<p>${p.replace(/\n/g,'<br>')}</p>`).join('')}
function cleanSpeech(value){return String(value||'').replace(/[#*_•]+/g,' ').replace(/\s+/g,' ').trim()}
function newsSpeechText(n){const details=cleanSpeech(n.details||n.tagline).split(/(?<=[.!?])\s+/).slice(0,10).join(' ');return [`IDI App Financial News.`, `Here is your professional news briefing.`, `${n.category||'Financial News'}.`, `${cleanSpeech(n.headline)}.`, `${cleanSpeech(n.tagline)}.`, `Reported by ${n.author||n.source}. Published ${formatNewsDate(n.publishAt)}.`, `Now, the details.`, details, `That was the latest update from IDI App Financial News.`].filter(Boolean).join(' ... ')}
function bestNewsVoice(){const voices=window.speechSynthesis?.getVoices?.()||[];const preferred=['Samantha','Karen','Daniel','Moira','Microsoft Aria','Microsoft Jenny','Google UK English Female','Google US English','Alex'];return voices.find(v=>preferred.some(name=>v.name.includes(name)))||voices.find(v=>/^en[-_]/i.test(v.lang)&&/female|natural|premium|enhanced/i.test(v.name))||voices.find(v=>/^en[-_]/i.test(v.lang))||voices[0]}
function startNewsAudio(index){const n=newsItems[index];if(!n){return}if(!('speechSynthesis'in window)){showToast('Audio not supported','This browser does not support text-to-speech.');return}window.speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(newsSpeechText(n));const voice=bestNewsVoice();if(voice){utterance.voice=voice;utterance.lang=voice.lang||'en-US'}else{utterance.lang='en-US'}utterance.rate=.84;utterance.pitch=.96;utterance.volume=1;utterance.onstart=()=>showToast('Professional news reader',n.headline);utterance.onend=()=>document.querySelectorAll('.news-audio-status').forEach(item=>item.textContent='Ready to listen');window.speechSynthesis.speak(utterance);document.querySelectorAll('.news-audio-status').forEach(item=>item.textContent=`Reading with ${voice?.name||'system voice'}…`)}
function pauseNewsAudio(){if('speechSynthesis'in window&&window.speechSynthesis.speaking)window.speechSynthesis.paused?window.speechSynthesis.resume():window.speechSynthesis.pause();document.querySelectorAll('.news-audio-status').forEach(item=>item.textContent=window.speechSynthesis?.paused?'Paused':'Reading article…')}
function stopNewsAudio(){if('speechSynthesis'in window)window.speechSynthesis.cancel();document.querySelectorAll('.news-audio-status').forEach(item=>item.textContent='Ready to listen')}
const newsArticleModal=document.getElementById('newsArticleModal');
function openNewsArticle(index){const n=newsItems[index];if(!n)return;const gallery=n.photos?.length?`<section class="news-photo-gallery">${n.photos.map((photo,i)=>`<figure style="background-image:url('${photo}')"><figcaption>Article photo ${i+1}</figcaption></figure>`).join('')}</section>`:'';document.getElementById('newsArticleContent').innerHTML=`<header class="news-story-header"><span class="story-section">${n.category||'Investment News'}</span><h1>${n.headline}</h1><p class="story-tagline">${n.tagline||''}</p><div class="story-byline"><div class="story-author">${initials(n.author||n.source)}</div><div><strong>By ${n.author||n.source}</strong><span>${formatNewsDate(n.publishAt)} • ${n.dateline||'Cambodia'} • ${Math.max(2,Math.ceil(String(n.details||'').split(/\s+/).length/180))} min read</span></div></div><div class="news-audio-player"><div><span>PROFESSIONAL READER MODE</span><strong>Listen as a news briefing</strong><small class="news-audio-status">Ready to listen</small></div><div><button class="primary" data-news-audio-play="${index}">▶ Play briefing</button><button class="secondary" data-news-audio-pause>Pause</button><button class="secondary" data-news-audio-stop>Stop</button></div></div></header><figure class="news-story-hero ${n.heroImage||n.thumbnail?'has-image':''}" ${n.heroImage||n.thumbnail?`style="background-image:url('${n.heroImage||n.thumbnail}')"`:''}><span>UNI VENTURECONNECT NEWS</span></figure>${n.imageCaption?`<p class="story-caption">${n.imageCaption}</p>`:''}<div class="news-story-layout"><main class="story-body">${newsParagraphs(n.details||n.tagline)}${gallery}<div class="story-hashtags">${(n.hashtags||[]).map(tag=>`<span>${tag}</span>`).join('')}</div></main><aside class="story-sidebar"><strong>Story information</strong><dl><dt>Source</dt><dd>${n.source}</dd><dt>Classification</dt><dd>Level ${n.level} — ${n.level===3?'Restricted':n.level===2?'Strategic':'Public'}</dd><dt>Audience</dt><dd>${n.audiences.join(', ')}</dd><dt>Distribution</dt><dd>${n.channels.join(', ')}</dd></dl><button class="primary story-share">Share article</button></aside></div>`;newsArticleModal.classList.add('open');newsArticleModal.setAttribute('aria-hidden','false');document.querySelector('.story-share').onclick=()=>showToast('Article link ready','The secure news link is ready to share with eligible members.');document.querySelector('[data-news-audio-play]').onclick=()=>startNewsAudio(index);document.querySelector('[data-news-audio-pause]').onclick=pauseNewsAudio;document.querySelector('[data-news-audio-stop]').onclick=stopNewsAudio}
function closeNewsArticle(){stopNewsAudio();newsArticleModal.classList.remove('open');newsArticleModal.setAttribute('aria-hidden','true')}
const openNewsArticleBase=openNewsArticle;openNewsArticle=function(index){openNewsArticleBase(index);const label=document.querySelector('.news-story-hero>span');if(label)label.textContent='IDI APP NEWS'};
document.querySelectorAll('[data-article-close]').forEach(b=>b.onclick=closeNewsArticle);
newsArticleModal.onclick=e=>{if(e.target===newsArticleModal)closeNewsArticle()};

function renderPartners(){
  const q=document.getElementById('partnerSearch').value.toLowerCase(),type=document.getElementById('partnerType').value,sector=document.getElementById('partnerSector').value;
  const rows=partnerOrganizations.filter(p=>(p.name+p.focus+p.capability+p.sectors.join(' ')).toLowerCase().includes(q)&&(type==='all'||p.type===type)&&(sector==='all'||p.sectors.includes(sector)));
  document.getElementById('partnerGrid').innerHTML=rows.map(p=>`<article class="partner-card"><div class="partner-top"><span class="partner-logo" style="background:${p.color}">${initials(p.name)}</span><div><h3>${p.name}</h3><p>${p.type} • ${p.location}</p></div>${p.verified===false?'<span class="status potential">Pending verification</span>':'<span class="verified">●</span>'}</div><p class="partner-focus">${p.focus}</p><div class="tags">${p.sectors.map(s=>`<span class="tag">${s}</span>`).join('')}</div><div class="partner-facts"><div><span>Investment / Value</span><strong>${p.ticket}</strong></div><div><span>Preferred stage</span><strong>${p.stage}</strong></div><div><span>Core capability</span><strong>${p.capability}</strong></div></div><div class="partner-actions"><button class="secondary campaign-create" data-name="${p.name}">Create campaign</button><button class="primary meeting-request" data-name="${p.name}">Request meeting</button></div></article>`).join('')||'<p>No organizations match these filters.</p>';
  document.querySelectorAll('.meeting-request').forEach(b=>b.onclick=()=>showToast('Meeting request created',`${b.dataset.name} will receive an in-app and email notification.`));
  document.querySelectorAll('.campaign-create').forEach(b=>b.onclick=()=>openCampaignModal(b.dataset.name));
}
['partnerSearch','partnerType','partnerSector'].forEach(id=>document.getElementById(id).addEventListener('input',renderPartners));
const partnerOnboardingModal=document.getElementById('partnerOnboardingModal');
function openPartnerOnboarding(){partnerOnboardingModal.classList.add('open');partnerOnboardingModal.setAttribute('aria-hidden','false')}
function closePartnerOnboarding(){partnerOnboardingModal.classList.remove('open');partnerOnboardingModal.setAttribute('aria-hidden','true')}
document.getElementById('addPartner').onclick=openPartnerOnboarding;
document.querySelectorAll('[data-partner-onboarding-close]').forEach(button=>button.onclick=closePartnerOnboarding);
document.getElementById('partnerOnboardingForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),sectors=String(form.get('sectors')).split(',').map(value=>value.trim()).filter(Boolean);partnerOrganizations.unshift({name:form.get('name'),type:form.get('type'),location:form.get('location'),sectors,focus:form.get('focus'),ticket:form.get('ticket'),stage:form.get('stage'),capability:form.get('capability'),website:form.get('website'),contact:form.get('contact'),email:form.get('email'),color:'#173f96',verified:false});renderPartners();renderPublicPartners();closePartnerOnboarding();showToast('Partner profile submitted',`${form.get('name')} is pending organization and mandate verification.`);e.target.reset()};
const campaignModal=document.getElementById('campaignModal');
function openCampaignModal(name){campaignModal.classList.add('open');campaignModal.querySelector('[name="organization"]').value=name;document.getElementById('campaignOwner').textContent=name}
function closeCampaignModal(){campaignModal.classList.remove('open')}
document.querySelectorAll('[data-campaign-close]').forEach(b=>b.onclick=closeCampaignModal);
document.getElementById('campaignForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),audiences=f.getAll('campaignAudience');if(!audiences.length){showToast('Select campaign audience','Choose at least one membership type.');return}closeCampaignModal();showToast('Campaign launched',`${f.get('campaign')} is visible to ${audiences.join(', ')} members.`);e.target.reset()};
function renderMembers(){
  const q=document.getElementById('memberSearch').value.toLowerCase();
  document.getElementById('memberTable').innerHTML=members.filter(m=>(m.name+m.email+m.org+m.tier+m.status).toLowerCase().includes(q)).map(m=>{
    const statusStyle=m.status==='Active'?'ready':m.status==='Expiring'||m.status==='Pending'?'potential':m.status==='Expired'?'improve':'overdue-status';
    return `<tr><td><div class="table-company"><div class="avatar">${initials(m.name)}</div><div><strong>${m.name} ${isMemberVerified(m)?verifiedBadge('member'):''}</strong><span>${m.email}</span></div></div></td><td>${m.org}</td><td><span class="membership-badge ${m.tier.toLowerCase()}">${m.tier}</span></td><td><span class="status ${statusStyle}">${m.status}</span></td><td>${m.renewal}</td><td>${m.channels}</td><td>${m.status==='Active'?`<button class="row-action" data-member-manage="${m.email}">⋯</button>`:m.status==='Pending'?`<button class="text-btn" data-member-review="${m.email}">Review</button>`:`<button class="text-btn member-renew-reminder" data-renew-member="${m.email}">Remind</button>`}</td></tr>`;
  }).join('');
  document.querySelectorAll('[data-renew-member]').forEach(button=>button.onclick=()=>showToast('Renewal reminder sent',`Email and in-app renewal notice sent to ${button.dataset.renewMember}.`));
  document.querySelectorAll('[data-member-manage]').forEach(button=>button.onclick=()=>showToast('Member management',`${button.dataset.memberManage}: profile, membership, payment, verification, and access controls opened.`));
  document.querySelectorAll('[data-member-review]').forEach(button=>button.onclick=()=>showToast('Application review opened',`${button.dataset.memberReview}: review identity, organization details, documents, payment, and approval.`));
}
document.getElementById('memberSearch').oninput=renderMembers;
document.getElementById('addMember').onclick=()=>openMemberRegistration();
document.getElementById('sendRenewalReminders').onclick=()=>showToast('Renewal reminders queued','105 scheduled notifications will be sent by membership status and due date.');

const adminRoles={
  'Super Admin':{icon:'◆',description:'Complete platform, security, billing, and staff control.',permissions:['All platform access','Manage staff','Security settings','Billing control']},
  'Membership Manager':{icon:'♙',description:'Reviews applicants, membership status, and renewals.',permissions:['Members','Applications','Renewals','Notifications']},
  'Directory Reviewer':{icon:'▦',description:'Verifies company profiles, products, and public listings.',permissions:['Company review','Approve listings','Edit directory','Media review']},
  'Content Editor':{icon:'▥',description:'Creates news and controls targeted content distribution.',permissions:['Publish news','Edit homepage','Send notifications','Media library']},
  'Finance Officer':{icon:'▣',description:'Manages invoices, payments, reconciliation, and reports.',permissions:['Invoices','Payments','ABA PayWay','Finance reports']},
  'Events Manager':{icon:'◷',description:'Creates events, invitations, registration, and attendance.',permissions:['Events','Registrations','Invitations','Attendance']}
};
let adminStaff=JSON.parse(localStorage.getItem('idi-admin-staff')||'null')||[
  {name:'Sokha Kim',email:'admin@idiapp.org',role:'Super Admin',status:'Active',lastActive:'Now'},
  {name:'Sophea Chan',email:'membership@idiapp.org',role:'Membership Manager',status:'Active',lastActive:'12 min ago'},
  {name:'Dara Vong',email:'directory@idiapp.org',role:'Directory Reviewer',status:'Active',lastActive:'1 hr ago'},
  {name:'Maly Touch',email:'news@idiapp.org',role:'Content Editor',status:'Active',lastActive:'Yesterday'},
  {name:'Ratha Lim',email:'finance@idiapp.org',role:'Finance Officer',status:'Active',lastActive:'Yesterday'},
  {name:'Sreypov Keo',email:'events@idiapp.org',role:'Events Manager',status:'Invited',lastActive:'Invitation pending'}
];
function renderRoleCards(){document.getElementById('roleCardGrid').innerHTML=Object.entries(adminRoles).map(([name,role])=>`<article class="role-card"><div class="role-card-head"><span class="role-icon">${role.icon}</span><div><h3>${name}</h3><p>${adminStaff.filter(staff=>staff.role===name).length} assigned staff</p></div></div><p>${role.description}</p><div class="permission-tags">${role.permissions.map(permission=>`<span>${permission}</span>`).join('')}</div></article>`).join('')}
function renderAdminStaff(){const role=document.getElementById('staffRoleFilter').value,rows=adminStaff.filter(staff=>role==='all'||staff.role===role);document.getElementById('staffTable').innerHTML=rows.map(staff=>`<tr><td><div class="table-company"><div class="avatar">${initials(staff.name)}</div><div><strong>${staff.name}</strong><span>${staff.email}</span></div></div></td><td><span class="staff-role">${staff.role}</span></td><td>${adminRoles[staff.role].permissions.slice(0,2).join(', ')}</td><td><span class="staff-status ${staff.status.toLowerCase()}">${staff.status}</span></td><td>${staff.lastActive}</td><td><button class="row-action staff-edit" data-staff-email="${staff.email}">⋯</button></td></tr>`).join('');document.getElementById('staffTotal').textContent=adminStaff.length;document.getElementById('staffActive').textContent=adminStaff.filter(staff=>staff.status==='Active').length;document.getElementById('staffInvited').textContent=adminStaff.filter(staff=>staff.status==='Invited').length;document.querySelectorAll('[data-staff-email]').forEach(button=>button.onclick=()=>showToast('Staff access controls',`Role, permissions, 2FA, and account status can be updated for ${button.dataset.staffEmail}.`));renderRoleCards()}
const staffModal=document.getElementById('staffModal');
function renderStaffPermissionPreview(){const role=adminRoles[document.getElementById('staffRoleSelect').value];document.getElementById('staffPermissionPreview').innerHTML=role.permissions.map(permission=>`<label><input type="checkbox" checked disabled /> ${permission}</label>`).join('')}
function openStaffModal(){renderStaffPermissionPreview();staffModal.classList.add('open')}
function closeStaffModal(){staffModal.classList.remove('open')}
document.getElementById('inviteAdminStaff').onclick=openStaffModal;
document.getElementById('staffRoleSelect').onchange=renderStaffPermissionPreview;
document.getElementById('staffRoleFilter').onchange=renderAdminStaff;
document.querySelectorAll('[data-staff-close]').forEach(button=>button.onclick=closeStaffModal);
document.getElementById('staffForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),email=String(form.get('email'));if(adminStaff.some(staff=>staff.email.toLowerCase()===email.toLowerCase())){showToast('Staff account already exists','Use the staff table to update this account.');return}adminStaff.push({name:form.get('name'),email,role:form.get('role'),status:'Invited',lastActive:'Invitation pending'});localStorage.setItem('idi-admin-staff',JSON.stringify(adminStaff));closeStaffModal();renderAdminStaff();showToast('Secure invitation sent',`${form.get('name')} was invited as ${form.get('role')}.`);e.target.reset()};

const memberRegistrationModal=document.getElementById('memberRegistrationModal');
function openMemberRegistration(plan='Standard'){closeLogin();memberRegistrationModal.classList.add('open');memberRegistrationModal.setAttribute('aria-hidden','false');document.getElementById('memberRegistrationForm').style.display='block';document.getElementById('memberRegistrationSuccess').classList.remove('show');const option=memberRegistrationModal.querySelector(`[name="membership"][value="${plan}"]`);if(option)option.checked=true}
function closeMemberRegistration(){memberRegistrationModal.classList.remove('open');memberRegistrationModal.setAttribute('aria-hidden','true')}
document.getElementById('publicMemberRegister').onclick=()=>openMemberRegistration();
document.getElementById('joinPlatform').onclick=()=>openMemberRegistration();
document.getElementById('openMemberRegistration').onclick=()=>openMemberRegistration();
document.querySelectorAll('[data-register-plan]').forEach(button=>button.onclick=()=>openMemberRegistration(button.dataset.registerPlan));
document.querySelectorAll('[data-member-register-close]').forEach(button=>button.onclick=closeMemberRegistration);
document.getElementById('publicCompanyRegister').onclick=openCompanyListing;
document.getElementById('memberRegistrationForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),password=String(form.get('password')),confirmPassword=String(form.get('confirmPassword'));if(password!==confirmPassword){showToast('Passwords do not match','Please enter the same password in both fields.');return}const email=String(form.get('email')).toLowerCase(),applications=JSON.parse(localStorage.getItem('idi-member-applications')||'[]');if(applications.some(application=>application.email===email)||members.some(member=>member.email.toLowerCase()===email)){showToast('Email already registered','Sign in or contact the membership team for assistance.');return}const reference=`IDI-MEM-${String(Date.now()).slice(-6)}`,application={reference,name:form.get('fullName'),email,phone:form.get('phone'),position:form.get('position'),applicantType:form.get('applicantType'),organization:form.get('organization')||'Individual',industry:form.get('industry'),location:form.get('location'),website:form.get('website'),membership:form.get('membership'),status:'Pending verification',submittedAt:new Date().toISOString()};applications.push(application);localStorage.setItem('idi-member-applications',JSON.stringify(applications));members.unshift({name:application.name,email:application.email,org:application.organization,tier:application.membership,status:'Pending',renewal:'After approval',channels:'Email',verified:false});renderMembers();document.getElementById('memberRegistrationReference').textContent=reference;e.target.style.display='none';document.getElementById('memberRegistrationSuccess').classList.add('show');showToast('Application submitted','Your membership application is now pending verification.');e.target.reset()};

const investmentStages=[
  {name:'Verified onboarding',duration:'1–5 days',owner:'Compliance lead',gate:'Identity cleared',companyAction:'Submit KYB, owners, authority',investorAction:'Submit KYC, mandate, source of funds',documents:['Registration and licenses','Directors and beneficial owners','Authorized signatory evidence','Investor mandate and fund profile'],checks:['Identity and authority verified','Sanctions, PEP and adverse-media screening','Conflicts disclosed by both parties','Data privacy consent recorded']},
  {name:'Funding request & mandate',duration:'2–7 days',owner:'Investment manager',gate:'Mandate fit confirmed',companyAction:'Define ask, use of funds, instrument',investorAction:'Define ticket, sectors, stage, geography',documents:['Funding request','Pitch deck and financial model','Investment mandate','Eligibility and exclusion criteria'],checks:['Funding need is specific','Investor has authority and capacity','Instrument and currency identified','No mandate conflict']},
  {name:'Match & mutual interest',duration:'2–10 days',owner:'Relationship manager',gate:'Both parties opt in',companyAction:'Review investor profile and consent',investorAction:'Review teaser and express interest',documents:['Blind teaser','Match rationale','Conflict declaration','Mutual interest record'],checks:['Company accepts introduction','Investor accepts introduction','Conflicts cleared','Communication owner assigned']},
  {name:'NDA & secure data room',duration:'1–5 days',owner:'Legal coordinator',gate:'Confidential access active',companyAction:'Sign NDA and upload indexed files',investorAction:'Sign NDA and name review team',documents:['Mutual NDA','Data-room index','Access permissions','Q&A protocol'],checks:['NDA executed by authorized signatories','Least-privilege access applied','Documents version-controlled','Audit log activated']},
  {name:'Screening & indication',duration:'1–3 weeks',owner:'Deal lead',gate:'IOI / screening approval',companyAction:'Pitch, answer questions, confirm terms',investorAction:'Initial commercial and risk screening',documents:['Management presentation','Screening memo','Indicative valuation','Indication of interest'],checks:['Business and market fit assessed','Financial quality reviewed','Key risks logged','Proceed / decline decision recorded']},
  {name:'Term sheet',duration:'1–3 weeks',owner:'Deal lead + counsel',gate:'Term sheet signed',companyAction:'Negotiate economics and governance',investorAction:'Propose structure and conditions',documents:['Term sheet','Capitalization table','Valuation analysis','Exclusivity / no-shop terms'],checks:['Economics approved','Governance and reserved matters agreed','Conditions precedent listed','Non-binding / binding clauses identified']},
  {name:'Full due diligence',duration:'3–8 weeks',owner:'DD workstream leads',gate:'All red flags resolved',companyAction:'Respond across all workstreams',investorAction:'Run legal, financial, tax, ESG review',documents:['Financial and tax reports','Legal and IP report','Commercial / technical report','ESG and impact assessment'],checks:['Material findings classified','Remediation owners and deadlines set','Data verified against source evidence','Final DD report accepted']},
  {name:'IC & final approval',duration:'1–2 weeks',owner:'Investment committee secretary',gate:'Documented approval',companyAction:'Confirm final disclosures and CP plan',investorAction:'Submit memo and vote independently',documents:['Investment memorandum','Risk and ESG summary','Final term comparison','Committee minutes / resolution'],checks:['Quorum and conflicts recorded','Decision rationale documented','Approval limits respected','Conditions and expiry date recorded']},
  {name:'Legal closing & funds',duration:'2–6 weeks',owner:'Transaction counsel',gate:'Funds and securities confirmed',companyAction:'Satisfy CPs and issue securities',investorAction:'Final AML check and transfer funds',documents:['Definitive agreements','Board / shareholder approvals','Closing checklist and CP evidence','Payment and ownership confirmation'],checks:['Legal documents executed','Bank and source-of-funds verified','Funds received in verified account','Registry / cap table updated']},
  {name:'Monitoring, value creation & exit',duration:'Investment life',owner:'Portfolio manager',gate:'Ongoing governance',companyAction:'Report KPIs, covenants and impact',investorAction:'Monitor, support, follow-on or exit',documents:['Reporting pack','Board materials','Covenant and impact reports','Exit / repayment documentation'],checks:['Reporting calendar active','Issues and remediation tracked','Valuation and ownership updated','Exit proceeds and lessons recorded']}
];
let activeInvestmentStage=0;
let investmentCases=[
  {company:'Mekong AgriTech',investor:'Impact Asia Capital',instrument:'Equity',stage:6,next:'Upload tax DD responses • 3 Jul',risk:'Medium'},
  {company:'Koompi Digital',investor:'Cambodia Innovation Fund',instrument:'Convertible note',stage:5,next:'Founder term-sheet review • 2 Jul',risk:'Low'},
  {company:'Sabay Logistics',investor:'Mekong Growth Partners',instrument:'Equity',stage:4,next:'Investor screening call • 4 Jul',risk:'Medium'},
  {company:'Tonle Finance',investor:'UNI Holding',instrument:'Strategic equity',stage:7,next:'IC decision meeting • 6 Jul',risk:'High'},
  {company:'GreenBuild Cambodia',investor:'Cambodia Innovation Fund',instrument:'Project finance',stage:2,next:'Confirm mutual interest • Today',risk:'Low'}
];
const investmentGates=[['KYB / KYC','Identity, authority, sanctions and PEP','Complete'],['Beneficial ownership','Ultimate natural-person owners verified','Complete'],['Investor source of funds','Origin and banking route verified','Complete'],['Conflicts & confidentiality','Declarations and NDA in audit trail','Complete'],['Legal / tax / financial DD','Material risks and remediation logged','Pending'],['ESG & impact review','Risk-based environmental and social assessment','Pending'],['Investment committee','Independent decision and limits recorded','Pending'],['Closing conditions','Executed documents, approvals and funds','Pending']];
function renderInvestmentFlow(){document.getElementById('investmentStageList').innerHTML=investmentStages.map((stage,index)=>`<button class="investment-stage ${index===activeInvestmentStage?'active':''}" data-investment-stage="${index}"><span class="stage-party company"><i>Co</i><span><strong>${stage.companyAction}</strong><small>Company responsibility</small></span></span><span class="stage-center"><span>${index+1}</span><strong>${stage.name}</strong><small>${stage.duration}</small></span><span class="stage-party investor"><span><strong>${stage.investorAction}</strong><small>Investor responsibility</small></span><i>Inv</i></span></button>`).join('');document.querySelectorAll('[data-investment-stage]').forEach(button=>button.onclick=()=>{activeInvestmentStage=Number(button.dataset.investmentStage);renderInvestmentFlow()});const stage=investmentStages[activeInvestmentStage];document.getElementById('investmentStageDetail').innerHTML=`<h2>Stage ${activeInvestmentStage+1}: ${stage.name}</h2><p>The case advances only when the gate owner confirms the evidence and both parties' required actions.</p><div class="stage-detail-meta"><div><span>ACCOUNTABLE OWNER</span><strong>${stage.owner}</strong></div><div><span>TARGET DURATION</span><strong>${stage.duration}</strong></div><div><span>EXIT GATE</span><strong>${stage.gate}</strong></div></div><div class="stage-checklists"><div><h3>Required documents</h3>${stage.documents.map(item=>`<p>${item}</p>`).join('')}</div><div><h3>Completion checklist</h3>${stage.checks.map(item=>`<p>${item}</p>`).join('')}</div></div>`;document.getElementById('investmentGateList').innerHTML=investmentGates.map((gate,index)=>`<div class="gate-item ${gate[2]==='Pending'?'pending':''}"><span>${gate[2]==='Complete'?'✓':'!'}</span><div><strong>${gate[0]}</strong><small>${gate[1]}</small></div><b>${gate[2]}</b></div>`).join('');renderInvestmentCases()}
function renderInvestmentCases(){const q=document.getElementById('investmentCaseSearch').value.toLowerCase(),rows=investmentCases.filter(item=>(item.company+item.investor+item.instrument+investmentStages[item.stage].name).toLowerCase().includes(q));document.getElementById('investmentCaseTable').innerHTML=rows.map((item,index)=>{const progress=(item.stage+1)*10;return `<tr><td><div class="table-company"><div class="logo" style="background:#173f96">⇄</div><div><strong>${item.company}</strong><span>${item.investor}</span></div></div></td><td>${item.instrument}</td><td>${investmentStages[item.stage].name}</td><td><div class="investment-progress"><div class="investment-progress-track"><i style="width:${progress}%"></i></div>${progress}%</div></td><td>${item.next}</td><td><span class="risk-pill ${item.risk.toLowerCase()}">${item.risk}</span></td><td><button class="row-action" data-investment-case="${index}">⋯</button></td></tr>`}).join('');document.getElementById('activeInvestmentCases').textContent=18+Math.max(0,investmentCases.length-5);document.querySelectorAll('[data-investment-case]').forEach(button=>button.onclick=()=>{const item=rows[Number(button.dataset.investmentCase)];showToast('Investment case controls',`${item.company} ↔ ${item.investor}: advance gate, request evidence, record decision, or close case.`)})}
const investmentCaseModal=document.getElementById('investmentCaseModal');
function openInvestmentCase(){investmentCaseModal.classList.add('open')}
function closeInvestmentCase(){investmentCaseModal.classList.remove('open')}
document.getElementById('startInvestmentCase').onclick=openInvestmentCase;
document.querySelectorAll('[data-investment-case-close]').forEach(button=>button.onclick=closeInvestmentCase);
document.getElementById('investmentCaseSearch').oninput=renderInvestmentCases;
document.getElementById('investmentCaseForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target);investmentCases.unshift({company:form.get('company'),investor:form.get('investor'),instrument:form.get('instrument'),stage:0,next:`Complete verified onboarding • ${form.get('targetDate')}`,risk:'Low'});closeInvestmentCase();renderInvestmentFlow();showToast('Investment case created',`${form.get('company')} and ${form.get('investor')} must now complete Gate 1 verification.`);e.target.reset()};

const opsFactorNames=['Founders','Market','Product','Traction','Financial','Innovation','Risk','ESG'];
let opsAssessments=[
  {company:'Mekong AgriTech',factors:[91,88,90,86,82,94,78,92]},
  {company:'Koompi Digital',factors:[89,84,92,88,80,95,82,76]},
  {company:'Sabay Logistics',factors:[82,90,85,91,86,79,74,72]},
  {company:'Tonle Finance',factors:[78,87,83,76,72,88,62,70]},
  {company:'GreenBuild Cambodia',factors:[69,81,72,58,55,84,60,88]},
  {company:'KhmerCraft Market',factors:[64,70,76,62,48,73,67,71]}
];
let opsCommittee=[
  {company:'Mekong AgriTech',score:88,owner:'Dr. Sokha Kim',meeting:'5 Jul 2026',status:'Ready for vote'},
  {company:'Koompi Digital',score:86,owner:'Rina Chen',meeting:'5 Jul 2026',status:'Ready for vote'},
  {company:'Sabay Logistics',score:82,owner:'Vireak S.',meeting:'8 Jul 2026',status:'Clarification'},
  {company:'Tonle Finance',score:77,owner:'Maly Touch',meeting:'8 Jul 2026',status:'Under review'}
];
const opsMatches=[
  {company:'Mekong AgriTech',investor:'Impact Asia Capital',fit:96,ticket:'$750K–$1.5M',factors:['Agriculture','Series A','Cambodia','Impact']},
  {company:'Koompi Digital',investor:'Cambodia Innovation Fund',fit:93,ticket:'$300K–$1M',factors:['Technology','Growth','Cambodia','Innovation']},
  {company:'Sabay Logistics',investor:'Mekong Growth Partners',fit:91,ticket:'$1M–$3M',factors:['Logistics','Growth','Mekong','Medium risk']},
  {company:'GreenBuild Cambodia',investor:'UNI Holding',fit:87,ticket:'Strategic',factors:['Green Economy','Project','Cambodia','Co-investment']},
  {company:'Tonle Finance',investor:'Impact Asia Capital',fit:84,ticket:'$500K–$2M',factors:['Fintech','Seed','ASEAN','Financial inclusion']},
  {company:'KhmerCraft Market',investor:'Ballangk Mall',fit:81,ticket:'Commercial',factors:['Commerce','Revenue','Cambodia','Distribution']}
];
let opsDocuments=[
  {name:'Mutual NDA',meta:'Signed by both parties • 30 Jun 2026',status:'E-signed',icon:'§'},
  {name:'Investment Term Sheet',meta:'Version 3 • Company review',status:'Pending',icon:'◇'},
  {name:'Shareholders Agreement',meta:'Counsel drafting',status:'Pending',icon:'⚖'},
  {name:'Capitalization Table',meta:'Verified against company registry',status:'Verified',icon:'▦'},
  {name:'Due Diligence Report',meta:'Legal, financial, tax, commercial, technology, ESG',status:'Verified',icon:'✓'},
  {name:'Financial Statements',meta:'FY2024–FY2026 + forecast',status:'Verified',icon:'▤'},
  {name:'Closing Checklist',meta:'3 conditions precedent outstanding',status:'Missing',icon:'!'}
];
let opsMilestones=[
  {name:'Closing payment',amount:'$400,000',condition:'Executed agreements and updated cap table',status:'Disbursed'},
  {name:'Market expansion',amount:'$300,000',condition:'Two provincial offices operational',status:'Ready'},
  {name:'Revenue milestone',amount:'$300,000',condition:'Quarterly revenue reaches $250K',status:'Locked'}
];
const opsPortfolio=[
  {company:'Mekong AgriTech',revenue:'+24%',runway:'18 months',valuation:'$6.8M',report:'Jun submitted',health:'On track'},
  {company:'Koompi Digital',revenue:'+18%',runway:'14 months',valuation:'$9.2M',report:'Jun submitted',health:'On track'},
  {company:'Sabay Logistics',revenue:'+11%',runway:'9 months',valuation:'$5.4M',report:'Action required',health:'Watch'},
  {company:'Tonle Finance',revenue:'+31%',runway:'16 months',valuation:'$4.7M',report:'Jun submitted',health:'On track'}
];
const opsSupport=[
  {name:'Investment Readiness Bootcamp',meta:'Financial model, valuation, governance • 6 sessions',audience:'Improvement Needed',icon:'✦'},
  {name:'Founder Mentorship Circle',meta:'Monthly leadership and growth coaching',audience:'Portfolio founders',icon:'◎'},
  {name:'Data Room Preparation Clinic',meta:'Legal, finance, tax and document readiness',audience:'Committee Review',icon:'▦'},
  {name:'Regional Market Access Program',meta:'Buyer, distributor and partner introductions',audience:'Investment Ready',icon:'↗'}
];
const opsExits=[
  {company:'Cambodia Solar Works',route:'Strategic acquisition',invested:'$650K',proceeds:'$1.82M',roi:'2.8x',irr:'36.4%'},
  {company:'KhmerPay Solutions',route:'Secondary sale',invested:'$400K',proceeds:'$920K',roi:'2.3x',irr:'29.1%'},
  {company:'AgriLink Network',route:'Founder buyback',invested:'$250K',proceeds:'$410K',roi:'1.6x',irr:'18.7%'}
];
let activeOpsTab='overview';
const opsScore=assessment=>Math.round(assessment.factors.reduce((sum,value)=>sum+value,0)/assessment.factors.length);
const opsCategory=score=>score>=85?['Investment Ready','']:score>=70?['Committee Review','review']:score>=55?['Improvement Needed','improve']:['Training Program','training'];
function renderInvestmentOperations(){document.querySelectorAll('[data-ops-tab]').forEach(button=>button.classList.toggle('active',button.dataset.opsTab===activeOpsTab));const content=document.getElementById('investmentOpsContent');if(activeOpsTab==='overview'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Workflow control center</h2><p>Items requiring action across the complete investment lifecycle</p></div><button class="secondary" data-ops-tab-jump="assessment">Review AI queue →</button></div><div class="workflow-alerts"><article class="workflow-alert"><strong>12 profiles incomplete</strong><p>Founder, traction, finance, or legal documents are missing.</p><button class="text-btn" data-ops-tab-jump="assessment">Open assessments</button></article><article class="workflow-alert"><strong>3 committee decisions due</strong><p>Quorum and conflict declarations must be recorded before voting.</p><button class="text-btn" data-ops-tab-jump="committee">Open committee</button></article><article class="workflow-alert success"><strong>2 milestones ready</strong><p>Conditions are verified and awaiting authorized fund release.</p><button class="text-btn" data-ops-tab-jump="funds">Open disbursement</button></article></div></section>`}
  if(activeOpsTab==='assessment'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>AI investment-readiness assessment</h2><p>Explainable scoring across founders, market, product, traction, financial health, innovation, risk, and ESG</p></div><button class="secondary" id="runAllAssessments">Run all assessments</button></div><div class="ops-grid">${opsAssessments.map((assessment,index)=>{const score=opsScore(assessment),category=opsCategory(score);return `<article class="ops-card"><div class="assessment-head"><div class="score-ring" style="--score:${score}"><strong>${score}</strong></div><div><h3>${assessment.company}</h3><span class="assessment-category ${category[1]}">${category[0]}</span></div></div><div class="factor-list">${assessment.factors.map((value,i)=>`<div class="factor-row"><span>${opsFactorNames[i]}</span><div class="factor-track"><i style="width:${value}%"></i></div><b>${value}</b></div>`).join('')}</div><div class="ops-action-row"><button class="secondary" data-assessment-report="${index}">View report</button><button class="primary" data-run-assessment="${index}">Re-score</button></div></article>`}).join('')}</div></section>`}
  if(activeOpsTab==='committee'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Investment committee queue</h2><p>AI evidence supports the decision; accountable committee members approve, return, or reject</p></div><button class="secondary">Schedule committee</button></div><div class="table-scroll"><table><thead><tr><th>Company</th><th>AI score</th><th>Case owner</th><th>Meeting</th><th>Status</th><th>Decision</th></tr></thead><tbody>${opsCommittee.map((item,index)=>`<tr><td><strong>${item.company}</strong></td><td>${item.score}/100</td><td>${item.owner}</td><td>${item.meeting}</td><td><span class="status ${item.status==='Approved'?'ready':'potential'}">${item.status}</span></td><td><button class="text-btn" data-committee-action="approve:${index}">Approve</button> <button class="text-btn" data-committee-action="return:${index}">Return</button></td></tr>`).join('')}</tbody></table></div></section>`}
  if(activeOpsTab==='matching'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Explainable investor matching</h2><p>Ranked by sector, ticket size, stage, geography, risk profile, and strategic value</p></div><button class="secondary">Refresh matching model</button></div><div class="ops-grid">${opsMatches.map((match,index)=>`<article class="ops-card"><div class="match-score"><div><h3>${match.company}</h3><p>${match.investor}</p></div><strong>${match.fit}%</strong></div><p>Mandate ticket: <b>${match.ticket}</b></p><div class="match-factors">${match.factors.map(factor=>`<span>${factor}</span>`).join('')}</div><div class="ops-action-row"><button class="secondary">View rationale</button><button class="primary" data-introduce-match="${index}">Request mutual interest</button></div></article>`).join('')}</div></section>`}
  if(activeOpsTab==='dealroom'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Secure digital deal room</h2><p>Version-controlled documents, access permissions, Q&A, and electronic signatures</p></div><button class="primary">＋ Upload document</button></div><div class="document-list">${opsDocuments.map((document,index)=>`<div class="document-row"><span>${document.icon}</span><div><strong>${document.name}</strong><small>${document.meta}</small></div><button class="doc-status ${document.status==='Pending'?'pending':document.status==='Missing'?'missing':''}" data-deal-document="${index}">${document.status}</button></div>`).join('')}</div></section>`}
  if(activeOpsTab==='funds'){const released=opsMilestones.filter(item=>item.status==='Disbursed').length;content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Milestone fund disbursement</h2><p>Dual authorization, verified bank details, conditions precedent, and payment audit trail</p></div><button class="secondary">Download disbursement ledger</button></div><div class="fund-summary"><div><span>APPROVED</span><strong>$1,000,000</strong></div><div><span>DISBURSED</span><strong>$${released===1?'400,000':'700,000'}</strong></div><div><span>REMAINING</span><strong>$${released===1?'600,000':'300,000'}</strong></div><div><span>COMPLETION</span><strong>${released===1?'40%':'70%'}</strong></div></div><div class="disbursement-bar"><i style="width:${released===1?'40':'70'}%"></i></div><div class="milestone-list">${opsMilestones.map((milestone,index)=>`<div class="milestone-row"><span>${index+1}</span><div><strong>${milestone.name} • ${milestone.amount}</strong><small>Release condition: ${milestone.condition}</small></div>${milestone.status==='Ready'?`<button class="primary" data-release-milestone="${index}">Release</button>`:`<span class="doc-status ${milestone.status==='Locked'?'missing':''}">${milestone.status}</span>`}</div>`).join('')}</div></section>`}
  if(activeOpsTab==='portfolio'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Portfolio performance & governance</h2><p>Monthly KPIs, quarterly reporting, annual audit, valuation, board actions, and covenants</p></div><button class="primary">＋ Request report</button></div><div class="table-scroll"><table><thead><tr><th>Portfolio company</th><th>Revenue growth</th><th>Runway</th><th>Latest valuation</th><th>Reporting</th><th>Health</th></tr></thead><tbody>${opsPortfolio.map(item=>`<tr><td><strong>${item.company}</strong></td><td>${item.revenue}</td><td>${item.runway}</td><td>${item.valuation}</td><td>${item.report}</td><td><span class="status ${item.health==='On track'?'ready':'potential'}">${item.health}</span></td></tr>`).join('')}</tbody></table></div><div class="kpi-mini-grid"><div><span>AVG REVENUE GROWTH</span><strong>21%</strong></div><div><span>REPORTING ON TIME</span><strong>86%</strong></div><div><span>BOARD ACTIONS OPEN</span><strong>9</strong></div><div><span>FOLLOW-ON RESERVED</span><strong>$1.4M</strong></div></div></section>`}
  if(activeOpsTab==='support'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Training, mentorship & value creation</h2><p>Route each company to the support program required by its assessment or portfolio plan</p></div><button class="primary">＋ Create support program</button></div><div class="support-list">${opsSupport.map(item=>`<div class="support-row"><span>${item.icon}</span><div><strong>${item.name}</strong><small>${item.meta}</small></div><button class="secondary" data-enroll-support="${item.name}">Enroll • ${item.audience}</button></div>`).join('')}</div></section>`}
  if(activeOpsTab==='exit'){content.innerHTML=`<section class="ops-panel"><div class="ops-panel-head"><div><h2>Exit & next-round management</h2><p>Acquisition, IPO, secondary sale, founder buyback, follow-on financing, ROI, and IRR</p></div><button class="primary">＋ Create exit scenario</button></div><div class="ops-grid">${opsExits.map(item=>`<article class="ops-card"><h3>${item.company}</h3><p>${item.route}</p><div class="exit-metric"><div><span>INVESTED</span><strong>${item.invested}</strong></div><div><span>PROCEEDS</span><strong>${item.proceeds}</strong></div><div><span>ROI</span><strong>${item.roi}</strong></div><div><span>IRR</span><strong>${item.irr}</strong></div></div><div class="ops-action-row"><button class="secondary">Exit checklist</button><button class="primary">View distribution</button></div></article>`).join('')}</div></section>`}
  content.querySelectorAll('[data-ops-tab-jump]').forEach(button=>button.onclick=()=>{activeOpsTab=button.dataset.opsTabJump;renderInvestmentOperations()});content.querySelectorAll('[data-run-assessment]').forEach(button=>button.onclick=()=>{const assessment=opsAssessments[Number(button.dataset.runAssessment)];assessment.factors=assessment.factors.map((value,index)=>Math.min(98,value+(index%3)));renderInvestmentOperations();showToast('AI assessment complete',`${assessment.company} was re-scored with an explainable factor report.`)});content.querySelectorAll('[data-assessment-report]').forEach(button=>button.onclick=()=>showToast('Assessment report ready','Evidence, confidence, risk flags, and recommended improvement actions are available.'));content.querySelectorAll('[data-committee-action]').forEach(button=>button.onclick=()=>{const [action,index]=button.dataset.committeeAction.split(':'),item=opsCommittee[Number(index)];item.status=action==='approve'?'Approved':'Clarification requested';renderInvestmentOperations();showToast('Committee decision recorded',`${item.company}: ${item.status}. Decision owner and timestamp added to the audit trail.`)});content.querySelectorAll('[data-introduce-match]').forEach(button=>button.onclick=()=>{const match=opsMatches[Number(button.dataset.introduceMatch)];showToast('Mutual-interest request sent',`${match.company} and ${match.investor} must both opt in before contact details are released.`)});content.querySelectorAll('[data-deal-document]').forEach(button=>button.onclick=()=>showToast('Document control opened','Version history, access log, comments, and e-signature status are available.'));content.querySelectorAll('[data-release-milestone]').forEach(button=>button.onclick=()=>{opsMilestones[Number(button.dataset.releaseMilestone)].status='Disbursed';renderInvestmentOperations();showToast('Milestone released','Dual approval and payment confirmation were recorded in the disbursement ledger.');});content.querySelectorAll('[data-enroll-support]').forEach(button=>button.onclick=()=>showToast('Enrollment started',`${button.dataset.enrollSupport} was added to the company support plan.`));document.getElementById('runAllAssessments')?.addEventListener('click',()=>showToast('Assessment queue started','All complete company profiles are being scored and routed by readiness category.'));content.querySelectorAll('button').forEach(button=>{if(button.id||Object.keys(button.dataset).length||button.onclick)return;button.onclick=()=>{const label=button.textContent.replace('＋','').trim();showToast(`${label} opened`,'The complete workflow, required approvals, records, and audit trail are ready in this workspace.')};})}
document.getElementById('opsTabs').onclick=e=>{const button=e.target.closest('[data-ops-tab]');if(!button)return;activeOpsTab=button.dataset.opsTab;renderInvestmentOperations()};
document.getElementById('opsNewApplication').onclick=openCompanyListing;

const deals=[
  {name:'GreenBuild Cambodia',investor:'Cambodia Innovation Fund',value:'$350K',stage:'New Lead',next:'Initial call • 2 Jul'},
  {name:'Lotus Health',investor:'Impact Asia Capital',value:'$500K',stage:'New Lead',next:'Review deck • 3 Jul'},
  {name:'Tonle Finance',investor:'Mekong Growth Partners',value:'$800K',stage:'Review',next:'Partner meeting • 1 Jul'},
  {name:'Sabay Logistics',investor:'Regional Logistics Fund',value:'$1.2M',stage:'Review',next:'Financial review • 5 Jul'},
  {name:'Mekong AgriTech',investor:'Impact Asia Capital',value:'$1.5M',stage:'Due Diligence',next:'Site visit • 8 Jul'},
  {name:'Koompi Digital',investor:'Cambodia Innovation Fund',value:'$900K',stage:'Due Diligence',next:'Legal review • 10 Jul'},
  {name:'KhmerCraft Market',investor:'ASEAN Commerce Fund',value:'$600K',stage:'Term Sheet',next:'Negotiate terms • 2 Jul'}
];
const dealStages=['New Lead','Review','Due Diligence','Term Sheet'];
function renderDeals(){const amount=value=>{const text=String(value||'').replace(/[$,\s]/g,''),num=parseFloat(text)||0;return /m/i.test(text)?num*1000000:/k/i.test(text)?num*1000:num};const money=value=>value>=1000000?`$${(value/1000000).toFixed(value>=10000000?0:1)}M`:`$${Math.round(value/1000)}K`;const probability={'New Lead':.25,Review:.45,'Due Diligence':.68,'Term Sheet':.82};const sectorMap={'GreenBuild Cambodia':'Green Construction','Lotus Health':'Healthcare','Tonle Finance':'Fintech','Sabay Logistics':'Logistics','Mekong AgriTech':'Agriculture','Koompi Digital':'Technology','KhmerCraft Market':'Commerce'};const board=document.getElementById('dealBoard');if(board){board.innerHTML=dealStages.map(stage=>{const rows=deals.filter(d=>d.stage===stage);return`<section class="kanban-column"><div class="kanban-head"><strong>${stage}</strong><span>${rows.length}</span></div>${rows.map(d=>`<article class="deal-card"><h3>${d.name}</h3><p>${d.investor}</p><div class="deal-value"><strong>${d.value}</strong><span>${stage}</span></div><div class="deal-next">◷ ${d.next}</div></article>`).join('')}</section>`}).join('')}const stageStats=dealStages.map(stage=>{const rows=deals.filter(d=>d.stage===stage),value=rows.reduce((sum,d)=>sum+amount(d.value),0);return{stage,count:rows.length,value,probability:Math.round((probability[stage]||.3)*100)}}),maxCount=Math.max(...stageStats.map(item=>item.count),1),maxValue=Math.max(...stageStats.map(item=>item.value),1);const funnel=document.getElementById('dealFunnelChart');if(funnel)funnel.innerHTML=stageStats.map((item,index)=>`<div class="funnel-row"><div><strong>${item.stage}</strong><span>${item.count} deals • ${item.probability}% probability</span></div><div class="funnel-track"><i style="width:${Math.max(14,item.count/maxCount*100-index*6)}%"></i></div><b>${money(item.value)}</b></div>`).join('');const valueChart=document.getElementById('dealStageValueChart');if(valueChart)valueChart.innerHTML=stageStats.map(item=>`<div class="stage-value-row"><span>${item.stage}</span><div class="stage-value-track"><i style="width:${Math.max(8,item.value/maxValue*100)}%"></i></div><strong>${money(item.value)}</strong></div>`).join('');const sectorTotals={};deals.forEach(deal=>{const sector=sectorMap[deal.name]||'Other';sectorTotals[sector]=(sectorTotals[sector]||0)+amount(deal.value)});const sectorMax=Math.max(...Object.values(sectorTotals),1);const sectorChart=document.getElementById('dealSectorChart');if(sectorChart)sectorChart.innerHTML=Object.entries(sectorTotals).sort((a,b)=>b[1]-a[1]).map(([sector,value])=>`<div class="sector-pill-row"><span>${sector}</span><div><i style="width:${Math.max(12,value/sectorMax*100)}%"></i></div><b>${money(value)}</b></div>`).join('');const watchlist=deals.filter(deal=>deal.stage==='Due Diligence'||deal.stage==='Term Sheet'||amount(deal.value)>=900000).slice(0,5);const risk=document.getElementById('dealRiskList');if(risk)risk.innerHTML=watchlist.map(deal=>{const value=amount(deal.value),level=deal.stage==='Term Sheet'?'Low':value>=1200000?'High':'Medium';const note=deal.stage==='Term Sheet'?'Prepare final approval and signing checklist.':value>=1200000?'Management review required for large ticket exposure.':'Follow due diligence evidence and investor Q&A.';return`<div class="risk-row ${level.toLowerCase()}"><span>${level}</span><div><strong>${deal.name}</strong><small>${note}</small></div><em>${deal.next}</em></div>`}).join('');const table=document.getElementById('dealPerformanceTable');if(table)table.innerHTML=deals.map(deal=>{const p=probability[deal.stage]||.3,value=amount(deal.value);return`<tr><td><strong>${deal.name}</strong><small>${sectorMap[deal.name]||'Other'}</small></td><td>${deal.investor}</td><td><span class="status ${deal.stage==='Term Sheet'?'ready':deal.stage==='Due Diligence'?'potential':''}">${deal.stage}</span></td><td>${deal.value}</td><td>${Math.round(p*100)}%</td><td>${money(value*p)}</td><td>${deal.next}</td></tr>`}).join('')}
const dealModal=document.getElementById('dealModal');document.getElementById('createDeal').onclick=()=>dealModal.classList.add('open');document.querySelectorAll('[data-deal-close]').forEach(b=>b.onclick=()=>dealModal.classList.remove('open'));document.getElementById('dealForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);deals.push({name:f.get('dealName'),investor:f.get('investor'),value:f.get('value'),stage:f.get('stage'),next:`Next action • ${f.get('nextDate')}`});renderDeals();dealModal.classList.remove('open');showToast('Opportunity created',`${f.get('dealName')} was added to the pipeline.`);e.target.reset()};

const conversations=[
  {name:'Impact Asia Capital',initials:'IA',role:'Investor',preview:'Can we schedule due diligence next week?',time:'10:42 AM',unread:true,color:'#283851',messages:[['in','We reviewed the updated financial model and would like to schedule due diligence next week.','10:35 AM'],['out','Thank you. I will coordinate with the company and send available times.','10:38 AM'],['in','Tuesday or Wednesday afternoon would work well for our team.','10:42 AM']]},
  {name:'Mekong AgriTech',initials:'MA',role:'Premium Member',preview:'The updated pitch deck has been uploaded.',time:'Yesterday',unread:true,color:'#37a178',messages:[['in','The updated pitch deck and revenue documents have been uploaded.','Yesterday'],['out','Received. Our review team will update the application status.','Yesterday']]},
  {name:'Khmer Enterprise',initials:'KE',role:'Government Partner',preview:'Please share the final event guest list.',time:'Mon',unread:false,color:'#5b4bdb',messages:[['in','Please share the final event guest list before Friday.','Monday']]},
  {name:'Ballangk Mall',initials:'BM',role:'Strategic Partner',preview:'Campaign applications are now open.',time:'28 Jun',unread:false,color:'#d77a3d',messages:[['in','Our SME supplier campaign is now accepting applications.','28 Jun']]}
];
let activeConversation=null,activeMessageFilter='all';
function renderConversations(){const q=document.getElementById('messageSearch').value.toLowerCase();const rows=conversations.filter(c=>(c.name+c.role+c.preview).toLowerCase().includes(q)&&(activeMessageFilter==='all'||activeMessageFilter==='unread'&&c.unread||activeMessageFilter==='investors'&&c.role==='Investor'));document.getElementById('conversationList').innerHTML=rows.map(c=>{const i=conversations.indexOf(c);return `<div class="conversation-item ${activeConversation===i?'active':''}" data-conversation="${i}"><div class="avatar" style="background:${c.color};color:#fff">${c.initials}</div><div><strong>${c.name}</strong><p>${c.preview}</p><time>${c.role} • ${c.time}</time></div>${c.unread?'<i class="unread-dot"></i>':''}</div>`}).join('')||'<div class="page-complete-note"><span>✓</span><strong>No conversations in this view.</strong></div>';document.querySelectorAll('[data-conversation]').forEach(el=>el.onclick=()=>openConversation(Number(el.dataset.conversation)))}
document.querySelector('.message-tabs').onclick=e=>{const button=e.target.closest('button');if(!button)return;activeMessageFilter=button.textContent.trim().toLowerCase()==='investors'?'investors':button.textContent.trim().toLowerCase()==='unread'?'unread':'all';document.querySelectorAll('.message-tabs button').forEach(item=>item.classList.toggle('active',item===button));renderConversations()};
function openConversation(i){activeConversation=i;const c=conversations[i];c.unread=false;document.getElementById('conversationPanel').innerHTML=`<div class="conversation-header"><div class="avatar" style="background:${c.color};color:#fff">${c.initials}</div><div><strong>${c.name}</strong><span>● Online • ${c.role}</span></div><button class="secondary">View profile</button></div><div class="message-thread">${c.messages.map(m=>`<div class="bubble ${m[0]==='in'?'incoming':'outgoing'}">${m[1]}<time>${m[2]}</time></div>`).join('')}</div><form class="message-compose" id="messageCompose"><input name="reply" required placeholder="Write a reply…"/><button class="primary">Send</button></form>`;document.getElementById('messageCompose').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);c.messages.push(['out',f.get('reply'),'Just now']);openConversation(i);showToast('Message sent',`Your reply was sent to ${c.name}.`)};renderConversations()}
document.getElementById('messageSearch').oninput=renderConversations;
const newMessageModal=document.getElementById('newMessageModal');
function closeNewMessage(){newMessageModal.classList.remove('open')}
document.getElementById('newMessage').onclick=()=>newMessageModal.classList.add('open');
document.querySelectorAll('[data-new-message-close]').forEach(button=>button.onclick=closeNewMessage);
document.getElementById('newMessageForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),[name,role]=String(form.get('recipient')).split('|'),existing=conversations.find(item=>item.name===name),message=String(form.get('message'));if(existing){existing.messages.push(['out',message,'Just now']);existing.preview=message;existing.time='Just now';activeConversation=conversations.indexOf(existing)}else{conversations.unshift({name,initials:initials(name),role,preview:message,time:'Just now',unread:false,color:'#173f96',messages:[['out',message,'Just now']]});activeConversation=0}closeNewMessage();renderConversations();openConversation(activeConversation);showToast('Message sent',`${name} received an in-app${form.get('emailCopy')?' and email':''} notification.`);e.target.reset()};

let reportRecords=JSON.parse(localStorage.getItem('idi-report-records')||'null')||[['Monthly Ecosystem Performance','Generated 29 Jun 2026','Ready'],['Investment Pipeline Summary','Generated 28 Jun 2026','Ready'],['Event Engagement & Attendance','Scheduled 01 Jul 2026','Scheduled']];
function renderReports(){const sectors=[['Technology',82,'$4.2M'],['Agriculture',64,'$3.1M'],['Fintech',51,'$2.4M'],['Logistics',38,'$1.8M'],['Other',27,'$1.3M']];const sectorBars=document.getElementById('sectorBars');if(sectorBars)sectorBars.innerHTML=sectors.map(s=>`<div class="sector-bar"><span>${s[0]}</span><div class="sector-track"><i style="width:${s[1]}%"></i></div><b>${s[2]}</b></div>`).join('');const reportActivity=document.getElementById('reportActivity');if(reportActivity)reportActivity.innerHTML=reportRecords.slice(0,4).map(r=>`<div class="report-row"><strong>${r[0]}</strong><span>${r[1]}</span><em>${r[2]}</em></div>`).join('');const monthly=[['Jan',1.2,3580,61],['Feb',1.8,3860,66],['Mar',2.4,4210,70],['Apr',2.1,4590,72],['May',2.8,5010,76],['Jun',3.6,5516,78]],maxInvestment=Math.max(...monthly.map(item=>item[1]));const monthlyChart=document.getElementById('monthlyPerformanceChart');if(monthlyChart)monthlyChart.innerHTML=`<div class="trend-chart">${monthly.map(item=>`<div class="trend-column"><div class="trend-bars"><i class="investment" style="height:${item[1]/maxInvestment*100}%"></i><i class="engagement" style="height:${item[3]}%"></i></div><strong>${item[0]}</strong><span>$${item[1]}M</span><small>${item[3]}% active</small></div>`).join('')}</div><div class="chart-legend"><span><i class="legend-investment"></i>Investment facilitated</span><span><i class="legend-engagement"></i>Member engagement</span><span>Members: ${monthly.at(-1)[2].toLocaleString()}</span></div>`;const channels=[['App notification',96,72,18420],['Email',91,58,12860],['SMS',88,41,6210]],channelChart=document.getElementById('reportChannelChart');if(channelChart)channelChart.innerHTML=channels.map(row=>`<div class="channel-row"><div><strong>${row[0]}</strong><small>${row[3].toLocaleString()} sent</small></div><div class="dual-track"><span style="width:${row[1]}%"></span><i style="width:${row[2]}%"></i></div><b>${row[2]}% read</b></div>`).join('')+`<div class="chart-note">Best executive channel: App notification + email follow-up.</div>`;const events=[['Investment Forum',216,184,42],['Pitch Day',124,101,28],['Bootcamp',68,59,11]],eventChart=document.getElementById('eventPerformanceChart');if(eventChart)eventChart.innerHTML=events.map(item=>`<div class="event-metric-row"><div><strong>${item[0]}</strong><small>${item[1]} registrations • ${item[3]} VIP/Premium</small></div><div class="attendance-ring" style="--p:${Math.round(item[2]/item[1]*100)}"><span>${Math.round(item[2]/item[1]*100)}%</span></div></div>`).join('');const readTimes=[['6AM',24],['8AM',78],['10AM',91],['12PM',46],['2PM',58],['5PM',73],['8PM',62]],newsRead=document.getElementById('newsReadChart');if(newsRead)newsRead.innerHTML=`<div class="read-time-bars">${readTimes.map(item=>`<div><i style="height:${item[1]}%"></i><span>${item[0]}</span><b>${item[1]}</b></div>`).join('')}</div><div class="chart-note">Recommended send window: 8:00–10:00 AM for highest read activity.</div>`;const insights=[['Pipeline quality','7 active opportunities, 3 require management review','High','Review large-ticket due diligence weekly'],['Membership revenue','VIP/Premium renewal reminders should start 30 days before expiry','Medium','Automate renewal notification and ABA KHQR payment'],['News engagement','Financial news performs best in morning send window','High','Schedule priority news before 10 AM'],['Event conversion','Forum attendance is strong but follow-up meeting conversion needs tracking','Medium','Add post-event investor matching report']],insightTable=document.getElementById('managementInsightTable');if(insightTable)insightTable.innerHTML=insights.map(row=>`<div class="insight-row"><span class="${row[2].toLowerCase()}">${row[2]}</span><div><strong>${row[0]}</strong><small>${row[1]}</small></div><em>${row[3]}</em></div>`).join('');const risks=[['Data completeness',84,'16% of profiles still need verification documents'],['Renewal risk',31,'members expiring within 30 days'],['Pipeline delay',5,'deals waiting more than 10 days'],['Event follow-up',72,'attendees require post-event outreach']],riskSummary=document.getElementById('reportRiskSummary');if(riskSummary)riskSummary.innerHTML=risks.map(row=>`<div class="report-risk-item"><strong>${row[1]}</strong><div><span>${row[0]}</span><small>${row[2]}</small></div></div>`).join('')}
const reportModal=document.getElementById('reportModal');
function openReportModal(mode='schedule'){document.getElementById('reportModalTitle').textContent=mode==='archive'?'Report archive':mode==='export'?'Export report':'Schedule report';document.getElementById('reportForm').style.display=mode==='archive'?'none':'grid';document.getElementById('reportArchiveList').innerHTML=mode==='archive'?reportRecords.map(record=>`<div class="archive-item"><div><strong>${record[0]}</strong><span>${record[1]}</span></div><span>IDI APP reporting workspace</span><em>${record[2]}</em></div>`).join(''):'';reportModal.dataset.mode=mode;reportModal.classList.add('open')}
function closeReportModal(){reportModal.classList.remove('open')}
document.querySelectorAll('[data-report-close]').forEach(button=>button.onclick=closeReportModal);
document.getElementById('exportReport').onclick=()=>openReportModal('export');document.getElementById('scheduleReport').onclick=()=>openReportModal('schedule');document.getElementById('reportArchive').onclick=()=>openReportModal('archive');
document.getElementById('reportForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),mode=reportModal.dataset.mode,frequency=form.get('frequency'),name=`${form.get('type')} — ${form.get('period')}`,status=mode==='export'||frequency==='One time'?'Ready':'Scheduled',date=status==='Ready'?`Generated ${new Intl.DateTimeFormat('en-GB',{dateStyle:'medium'}).format(new Date())}`:`${frequency} delivery`;reportRecords.unshift([name,date,status]);localStorage.setItem('idi-report-records',JSON.stringify(reportRecords));renderReports();closeReportModal();showToast(status==='Ready'?'Report generated':'Report scheduled',`${name} is now available in the report archive.`);e.target.reset()};
document.getElementById('newsTabs').addEventListener('click',e=>{if(!e.target.dataset.newsFilter)return;activeNewsFilter=e.target.dataset.newsFilter;document.querySelectorAll('#newsTabs button').forEach(b=>b.classList.toggle('active',b===e.target));renderAdminNews()});
const newsModal=document.getElementById('newsModal');
function openNewsModal(){newsModal.classList.add('open');newsModal.setAttribute('aria-hidden','false');const field=newsModal.querySelector('[name="publishAt"]');if(field&&!field.value){const now=new Date();now.setMinutes(now.getMinutes()-now.getTimezoneOffset());field.value=now.toISOString().slice(0,16)}}
function closeNewsModal(){newsModal.classList.remove('open');newsModal.setAttribute('aria-hidden','true')}
document.getElementById('openNewsComposer').onclick=openNewsModal;
document.querySelectorAll('[data-news-close]').forEach(b=>b.onclick=closeNewsModal);
newsModal.onclick=e=>{if(e.target===newsModal)closeNewsModal()};
document.getElementById('selectAllAudience').onchange=e=>document.querySelectorAll('input[name="audience"]').forEach(x=>x.checked=e.target.checked);
document.getElementById('newsForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);const channels=['app','email','sms'].filter(c=>f.get(c));const audiences=f.getAll('audience');if(!audiences.length){showToast('Select an audience','Choose at least one membership type.');return}if(!channels.length){showToast('Select a delivery channel','Choose in-app, email, or SMS.');return}const fileUrl=file=>file&&file.size?URL.createObjectURL(file):'';const hashtags=String(f.get('hashtags')||'').split(/[\s,]+/).filter(Boolean).map(tag=>tag.startsWith('#')?tag:`#${tag}`);const publishAt=f.get('publishAt');const isFuture=new Date(publishAt)>new Date();newsItems.unshift({headline:f.get('headline'),tagline:f.get('tagline'),details:f.get('details'),category:f.get('category'),hashtags,author:f.get('author'),dateline:f.get('dateline'),publishAt,imageCaption:f.get('imageCaption'),thumbnail:fileUrl(f.get('thumbnail')),heroImage:fileUrl(f.get('heroImage')),photos:[...e.target.querySelector('[name="newsPhotos"]').files].map(fileUrl),sourceType:f.get('sourceType'),source:f.get('source'),level:Number(f.get('level')),audiences,channels,release:isFuture?`Scheduled ${formatNewsDate(publishAt)}`:'Published now'});renderAdminNews();closeNewsModal();showToast(isFuture?'Article scheduled':'Article published',`${audiences.length===4?'All membership types':audiences.join(', ')} will receive the professional news article.`);e.target.reset();document.getElementById('selectAllAudience').checked=false};

const membershipPrices={VIP:{USD:500,KHR:2000000,benefits:['All intelligence levels','Priority investor access','VIP events','SMS alerts','Exclusive campaigns']},Premium:{USD:250,KHR:1000000,benefits:['Level 1–2 news','Partner discovery','Pitching programs','Member events']},Standard:{USD:120,KHR:480000,benefits:['Member news','Verified directory','Public campaigns','Open events']}};
let activeBillingTier='Standard';
const formatBillingDate=value=>new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(`${value}T12:00:00`));
const dateKey=date=>{const y=date.getFullYear(),m=String(date.getMonth()+1).padStart(2,'0'),d=String(date.getDate()).padStart(2,'0');return `${y}-${m}-${d}`};
function effectiveExpiry(profile){return localStorage.getItem(`idi-membership-expiry-${profile.email}`)||profile.membershipExpires||''}
function daysToExpiry(value){if(!value)return null;const now=new Date();now.setHours(0,0,0,0);const expiry=new Date(`${value}T00:00:00`);return Math.round((expiry-now)/86400000)}
function renewalStage(days){if(days===null)return'upgrade';if(days>30)return'active';if(days>7)return'month';if(days>0)return'week';if(days===0)return'today';if(days>=-30)return'expired';return'overdue'}
function renewalCopy(stage,days){return{active:['Membership active',`${days} days remain in your current membership period.`],month:['Renewal due within one month',`Your membership expires in ${days} days. Renew early to keep uninterrupted access.`],week:['Urgent: membership expires soon',`Only ${days} day${days===1?'':'s'} remain. Complete renewal now to avoid an access interruption.`],today:['Membership expires today','Renew today to keep your current membership benefits and content access.'],expired:['Membership expired',`Your membership expired ${Math.abs(days)} day${Math.abs(days)===1?'':'s'} ago. Renew to restore full access.`],overdue:['Membership renewal overdue',`Your membership is ${Math.abs(days)} days overdue. Payment is required to reactivate member access.`],upgrade:['Upgrade from Guest access','Choose a paid membership to unlock member news, partners, campaigns, and events.']}[stage]}
function renderRenewalCenter(profile){if(profile.isAdmin)return;activeBillingTier=profile.tier==='Guest'?'Standard':profile.tier;const expiry=effectiveExpiry(profile),days=daysToExpiry(expiry),stage=renewalStage(days),copy=renewalCopy(stage,days);document.getElementById('billingStatusCard').className=`billing-status-card ${stage}`;document.getElementById('billingStatusLabel').textContent=stage==='upgrade'?'MEMBERSHIP UPGRADE':stage.toUpperCase().replace('MONTH','30-DAY NOTICE').replace('WEEK','7-DAY NOTICE');document.getElementById('billingStatusTitle').textContent=copy[0];document.getElementById('billingStatusText').textContent=copy[1];document.getElementById('billingDaysRemaining').textContent=days===null?'Choose a plan':days>0?`${days} days`:days===0?'Due today':`${Math.abs(days)} days late`;document.getElementById('billingPlanName').textContent=`${activeBillingTier} Membership`;document.getElementById('billingOrganization').textContent=profile.organization;document.getElementById('billingTierSeal').textContent=activeBillingTier==='VIP'?'◆':activeBillingTier[0];document.getElementById('billingExpiryDate').textContent=expiry?formatBillingDate(expiry):'Not applicable';const base=expiry&&new Date(`${expiry}T12:00:00`)>new Date()?new Date(`${expiry}T12:00:00`):new Date();const nextStart=new Date(base);nextStart.setDate(nextStart.getDate()+1);const nextEnd=new Date(nextStart);nextEnd.setFullYear(nextEnd.getFullYear()+Number(document.getElementById('renewalTerm').value));nextEnd.setDate(nextEnd.getDate()-1);document.getElementById('billingNewPeriod').textContent=`${new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(nextStart)} – ${new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(nextEnd)}`;document.getElementById('billingAccessStatus').textContent=stage==='expired'||stage==='overdue'?'Restricted':stage==='upgrade'?'Guest':'Active';document.getElementById('billingBenefits').innerHTML=membershipPrices[activeBillingTier].benefits.map(item=>`<span>✓ ${item}</span>`).join('');renderBillingAmounts();renderBillingHistory(profile)}
function renderBillingAmounts(){const term=Number(document.getElementById('renewalTerm').value),currency=document.getElementById('paymentCurrency').value,unit=membershipPrices[activeBillingTier][currency],total=unit*term,formatted=currency==='USD'?`$${total.toFixed(2)}`:`៛${total.toLocaleString('en-US')}`;document.getElementById('invoicePlanLabel').textContent=`${activeBillingTier} membership × ${term} year${term>1?'s':''}`;document.getElementById('invoiceSubtotal').textContent=formatted;document.getElementById('invoiceTotal').textContent=formatted;document.getElementById('khqrAmount').textContent=formatted;document.getElementById('khqrPlan').textContent=`${activeBillingTier} Membership • ${term} year${term>1?'s':''}`;if(currentMember)renderRenewalCenterPeriodOnly(currentMember,term)}
function renderRenewalCenterPeriodOnly(profile,term){const expiry=effectiveExpiry(profile),base=expiry&&new Date(`${expiry}T12:00:00`)>new Date()?new Date(`${expiry}T12:00:00`):new Date(),start=new Date(base);start.setDate(start.getDate()+1);const end=new Date(start);end.setFullYear(end.getFullYear()+term);end.setDate(end.getDate()-1);document.getElementById('billingNewPeriod').textContent=`${new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(start)} – ${new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(end)}`}
function renderBillingHistory(profile){const saved=JSON.parse(localStorage.getItem(`idi-payment-history-${profile.email}`)||'[]'),base=profile.tier==='Guest'?[]:[{invoice:'IDI-2025-0018',date:'28 Jul 2025',amount:profile.tier==='VIP'?'$500.00':profile.tier==='Premium'?'$250.00':'$120.00',method:'ABA KHQR',status:'Paid'}],rows=[...saved,...base];document.getElementById('billingHistory').innerHTML=rows.length?rows.map(row=>`<div class="billing-history-row"><strong>${row.invoice}</strong><span>${row.date}</span><span>${row.method}</span><em>${row.status}</em><button>Receipt</button></div>`).join(''):'<div class="billing-empty-history">No membership payments yet.</div>'}
document.getElementById('renewalTerm').onchange=renderBillingAmounts;document.getElementById('paymentCurrency').onchange=renderBillingAmounts;document.getElementById('downloadInvoice').onclick=()=>showToast('Invoice prepared','Your membership renewal invoice is ready for download in production.');
const khqrModal=document.getElementById('khqrModal');
function openKhqrCheckout(){const order=`IDI-RENEW-${Date.now().toString().slice(-8)}`;document.getElementById('khqrOrderId').textContent=`Order ${order}`;document.getElementById('paywayCheckoutLink').value='Generated by ABA PayWay after merchant activation';document.getElementById('khqrTimer').textContent='10:00';khqrModal.classList.add('open');khqrModal.setAttribute('aria-hidden','false')}
function closeKhqrCheckout(){khqrModal.classList.remove('open');khqrModal.setAttribute('aria-hidden','true')}
document.getElementById('generateKhqr').onclick=openKhqrCheckout;document.querySelectorAll('[data-khqr-close]').forEach(button=>button.onclick=closeKhqrCheckout);khqrModal.onclick=e=>{if(e.target===khqrModal)closeKhqrCheckout()};document.getElementById('copyCheckoutLink').onclick=()=>showToast('Merchant activation required','The real checkout_qr_url will be returned by the secure ABA PayWay API.');document.getElementById('abaDeeplink').onclick=e=>e.preventDefault();
document.getElementById('completeDemoPayment').onclick=()=>{if(!currentMember)return;const term=Number(document.getElementById('renewalTerm').value),expiry=effectiveExpiry(currentMember),base=expiry&&new Date(`${expiry}T12:00:00`)>new Date()?new Date(`${expiry}T12:00:00`):new Date(),newExpiry=new Date(base);newExpiry.setFullYear(newExpiry.getFullYear()+term);const newExpiryKey=dateKey(newExpiry);localStorage.setItem(`idi-membership-expiry-${currentMember.email}`,newExpiryKey);const history=JSON.parse(localStorage.getItem(`idi-payment-history-${currentMember.email}`)||'[]'),currency=document.getElementById('paymentCurrency').value,total=membershipPrices[activeBillingTier][currency]*term;history.unshift({invoice:`IDI-${new Date().getFullYear()}-${String(history.length+1).padStart(4,'0')}`,date:new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric'}).format(new Date()),amount:currency==='USD'?`$${total.toFixed(2)}`:`៛${total.toLocaleString('en-US')}`,method:'ABA KHQR (Demo)',status:'Paid'});localStorage.setItem(`idi-payment-history-${currentMember.email}`,JSON.stringify(history));closeKhqrCheckout();renderRenewalCenter(currentMember);showToast('Demo payment successful',`Membership renewed through ${formatBillingDate(newExpiryKey)}. Production will verify payment by PayWay callback and transaction-status API.`)};
document.getElementById('notificationButton').onclick=()=>{if(currentMember&&!currentMember.isAdmin&&currentMember.allowed.includes('billing'))navigate('billing');else showToast('Notifications','No personal renewal notice for this account.')};

const modal=document.getElementById('applicationModal');
function openModal(){calculateScore();modal.classList.add('open');modal.setAttribute('aria-hidden','false')}
function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}
['openApplication','openApplication2'].forEach(id=>document.getElementById(id).onclick=openModal);
document.querySelectorAll('[data-close]').forEach(b=>b.onclick=closeModal);
modal.onclick=e=>{if(e.target===modal)closeModal()};
function calculateScore(){const f=new FormData(document.getElementById('applicationForm'));const vals=['model','market','team','finance','product'].map(k=>Number(f.get(k)));const score=Math.round(vals.reduce((a,b)=>a+b,0)*2);document.getElementById('liveScore').textContent=`${score} / 100`;return score}
document.querySelectorAll('input[type=range]').forEach(r=>r.addEventListener('input',calculateScore));
document.getElementById('applicationForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),score=calculateScore();const status=score>=85?'Investor Ready':score>=70?'Strong Potential':'Need Improvement';pitches.unshift({name:f.get('company'),industry:f.get('industry'),stage:f.get('stage'),complete:80,score,status,date:'28 Jun 2026',color:'#5b4bdb'});renderPitches();closeModal();navigate('pitching');showToast('Application analyzed',`${f.get('company')} scored ${score}/100 — ${status}.`);e.target.reset();calculateScore()};

function showToast(title,msg){const t=document.getElementById('toast');t.querySelector('strong').textContent=title;t.querySelector('p').textContent=msg;t.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>t.classList.remove('show'),3200)}
const loginModal=document.getElementById('loginModal');
function openLogin(prefill){loginModal.classList.add('open');loginModal.setAttribute('aria-hidden','false');if(prefill&&loginProfiles[prefill])loginModal.querySelector('[name="email"]').value=loginProfiles[prefill].email}
function closeLogin(){loginModal.classList.remove('open');loginModal.setAttribute('aria-hidden','true')}
['headerLogin','heroLogin','footerLogin'].forEach(id=>document.getElementById(id).onclick=()=>openLogin());
document.getElementById('quickLogin').onclick=()=>openLogin();
const directoryIntro=document.querySelector('#public-directory .directory-intro'),directoryListButton=document.getElementById('listCompanyButton');
if(directoryIntro&&directoryListButton){
  const actions=document.createElement('div');
  actions.className='directory-intro-actions';
  actions.innerHTML='<button class="secondary directory-login-trigger"><img src="assets/idi-logo.jpg" alt="" /> Member login</button>';
  actions.append(directoryListButton);
  directoryIntro.append(actions);
  actions.querySelector('.directory-login-trigger').onclick=()=>openLogin();
  const legend=document.createElement('div');
  legend.className='verification-legend';
  legend.innerHTML=`${verifiedBadge()} <span>Blue tick means the company completed all requirements and was approved by an IDI administrator.</span>`;
  document.querySelector('#public-directory .public-directory-filters')?.insertAdjacentElement('beforebegin',legend);
}
document.getElementById('publicContactForm')?.addEventListener('submit',event=>{event.preventDefault();const form=new FormData(event.target);const inquiries=JSON.parse(localStorage.getItem('idi-contact-inquiries')||'[]');inquiries.unshift({name:form.get('name'),email:form.get('email'),type:form.get('type'),organization:form.get('organization'),message:form.get('message'),status:'New',submittedAt:new Date().toISOString()});localStorage.setItem('idi-contact-inquiries',JSON.stringify(inquiries));showToast('Contact request received','Your inquiry has been recorded for IDI admin follow-up.');event.target.reset()});
document.getElementById('adminLogin').onclick=()=>openLogin('admin');
document.querySelectorAll('[data-login-close]').forEach(b=>b.onclick=closeLogin);
document.querySelectorAll('[data-demo]').forEach(b=>b.onclick=()=>loginAs(b.dataset.demo));
function loginAs(key){const profile=loginProfiles[key]||loginProfiles.guest;currentMember=profile;localStorage.setItem('uni-member-session',key);document.body.classList.toggle('admin-session',!!profile.isAdmin);closeLogin();document.getElementById('landingPage').style.display='none';document.querySelectorAll('.app-shell').forEach(el=>el.classList.add('authenticated'));document.getElementById('sidebarName').textContent=profile.name;document.getElementById('sidebarInitials').textContent=initials(profile.name);document.getElementById('sidebarRole').textContent=`${profile.role} • ${profile.organization}`;document.getElementById('memberTierChip').textContent=profile.tier;document.querySelectorAll('.nav-item[data-page]').forEach(n=>n.classList.toggle('access-hidden',!profile.allowed.includes(n.dataset.page)));document.getElementById('openNewsComposer').style.display=profile.canPublish?'':'none';document.querySelector('.distribution-panel').style.display=profile.canPublish?'':'none';document.querySelectorAll('.campaign-create').forEach(b=>b.style.display=profile.canCampaign||profile.canPublish?'':'none');renderAdminNews();renderPublicDirectory();renderRenewalCenter(profile);renderFundingAccess(profile);navigate('investment-flow');if(listingRequested){listingRequested=false;openCompanyListing()}}
document.getElementById('loginForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),email=String(f.get('email')).toLowerCase(),password=String(f.get('password'));const match=Object.entries(loginProfiles).find(([,p])=>p.email.toLowerCase()===email);if(match?.[1].isAdmin&&password!==match[1].password){showToast('Admin login unsuccessful','Please check the administrator password.');return}loginAs(match?match[0]:'guest')};
document.getElementById('logoutButton').onclick=()=>{localStorage.removeItem('uni-member-session');currentMember=null;document.body.classList.remove('admin-session');document.querySelectorAll('.app-shell').forEach(el=>el.classList.remove('authenticated'));document.getElementById('landingPage').style.display='block';window.scrollTo({top:0,behavior:'smooth'})};
document.getElementById('runScreening').onclick=()=>{showToast('AI screening started','12 applications are being checked for completeness and quality.');document.getElementById('sidebar').classList.remove('open')};
document.getElementById('globalSearch').addEventListener('keydown',e=>{if(e.key==='Enter'&&typeof renderBusinessMapping==='function'){businessMapSearch=e.target.value;navigate('business-mapping');renderBusinessMapping()}});
document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();document.getElementById('globalSearch').focus()}if(e.key==='Escape'){closeModal();closeNewsModal();closeNewsArticle();closePublicEventRegistration();closePublicPartner();closeKhqrCheckout();closeCompanyProfile();closeCompanyListing();closeMemberRegistration();closeStaffModal();closeInvestmentCase();dealModal.classList.remove('open')}});

renderDirectory();renderPublicDirectory();renderPublicPartners();renderInvestmentFlow();renderInvestmentOperations();renderFundingRequests();renderCompanyVerification();renderPitches();renderEvents();renderPublicEvents();renderAdminNews();renderPartners();renderMembers();renderDeals();renderConversations();renderReports();renderAdminStaff();renderStaffPermissionPreview();
const adminOnlyLogin=loginAs;
loginAs=function(key){if(key!=='admin'){showToast('Administrator access required','Only the administrator interface is available in this deployment.');return}adminOnlyLogin('admin')};
document.body.classList.add('admin-only-mode');
document.querySelector('.login-logo p').textContent='Sign in to the administration portal';
document.getElementById('loginForm').onsubmit=e=>{e.preventDefault();const form=new FormData(e.target),admin=loginProfiles.admin;if(String(form.get('email')).toLowerCase()!==admin.email||String(form.get('password'))!==admin.password){showToast('Admin login unsuccessful','Please check the administrator credentials.');return}loginAs('admin')};
document.getElementById('logoutButton').onclick=()=>{localStorage.removeItem('uni-member-session');currentMember=null;document.body.classList.remove('admin-session');document.querySelectorAll('.app-shell').forEach(el=>el.classList.remove('authenticated'));openLogin('admin')};
loginAs('admin');
